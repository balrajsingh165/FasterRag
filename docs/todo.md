# todo.md — Universal Task File

> **This is the ONLY task file in fasterRag. No other todo/task/tracking file may ever be created. All tasks live here.**
>
> Rules:
> - Open task: `- [ ] TASK-0001: <description>`
> - Completed task: `- [x] TASK-0001: <description> — ✅ YYYY-MM-DD`
> - **Append-only rule:** when a task is completed, tick the checkbox and append the completion date in `YYYY-MM-DD` format. After ticking, that entry is append-only and must NOT be edited further.
> - Task IDs (`TASK-`/`AUDIT-`) are sequential and never reused. Audit-gap tasks use the `AUDIT-` prefix.
> - [blockers.md](blockers.md) is a **read-only view** of the entries below that are waiting on a human — it holds no task of its own and every line there cites an id from this file. It is not an exception to the rule above; it is a table of contents for one slice of it.

## Done

### Phase 1 — documentation set

- [x] TASK-0001: Author CLAUDE.md (always-loaded rules, tech stack, standards, pointers) — ✅ 2026-07-29
- [x] TASK-0002: Author scope.md (vision, non-goals, pain-point catalogue, success criteria) — ✅ 2026-07-29
- [x] TASK-0003: Author todo.md (this universal task file with entry format and append-only rule) — ✅ 2026-07-29
- [x] TASK-0004: Author structure.md (proposed repository layout and layer responsibilities) — ✅ 2026-07-29
- [x] TASK-0005: Author flow.md (Mermaid flows: ingestion, worker hand-off, query, cache, provisioning) — ✅ 2026-07-29
- [x] TASK-0006: Author architecture.md (workers, adapters, batching, caching, fault tolerance, scaling) — ✅ 2026-07-29
- [x] TASK-0007: Author config-reference.md (full config.yaml schema with types, defaults, validation) — ✅ 2026-07-29
- [x] TASK-0008: Author api-reference.md (REST endpoints, schemas, status codes, streaming semantics) — ✅ 2026-07-29
- [x] TASK-0009: Author cli-reference.md (all commands, subcommands, flags) — ✅ 2026-07-29
- [x] TASK-0010: Author observability.md (dashboard spec, metrics catalogue, Langfuse/Grafana provisioning) — ✅ 2026-07-29
- [x] TASK-0011: Author deployment.md (self-hosting, Docker modes, remote connections, sizing) — ✅ 2026-07-29
- [x] TASK-0012: Author testing-strategy.md (unit/integration tests, eval harness, CI gates) — ✅ 2026-07-29
- [x] TASK-0013: Author security.md (secrets, .env policy, auth, multi-tenancy, Qdrant API key) — ✅ 2026-07-29
- [x] TASK-0014: Author performance.md (benchmark targets and measurement methodology) — ✅ 2026-07-29
- [x] TASK-0015: Author integrations.md (vector DBs, embedding/LLM providers, observability tools) — ✅ 2026-07-29
- [x] TASK-0016: Author CHANGELOG.md (Keep a Changelog + SemVer 2.0.0) — ✅ 2026-07-29
- [x] TASK-0017: Author CONTRIBUTING.md (commit, comment, one-todo-file, incremental-shipping rules) — ✅ 2026-07-29
- [x] TASK-0018: Seed docs/adr/ with ADR-0001..ADR-0005 (MADR style) — ✅ 2026-07-29
- [x] TASK-0019: Review documentation set against Phase 1 acceptance criteria and fix gaps (executed as the Gate A audit, see AUDIT tasks) — ✅ 2026-07-29

### Phase 2 — restructure, framework surface, Gate B hardening

- [x] TASK-0056: Relocate all documentation into docs/ keeping only CLAUDE.md and README.md at root — ✅ 2026-07-29
- [x] TASK-0057: Author python-api.md (importable package surface, sync facade, standalone components, entry-point plugin contract) — ✅ 2026-07-29
- [x] TASK-0058: Author ADR-0006 (ship fasterRag as an importable Python package) — ✅ 2026-07-29
- [x] TASK-0059: Author detailed root README.md positioning fasterRag as a RAG framework — ✅ 2026-07-29
- [x] TASK-0060: Author differentiators.md (D1–D12, each with pain point, uniqueness, config keys, CLI/API surface, acceptance test, proof metric) — ✅ 2026-07-29
- [x] TASK-0061: Author reliability.md (doctrine sentence, error taxonomy, resilience patterns, data safety, static discipline) — ✅ 2026-07-29
- [x] TASK-0062: Author failure-modes.md (FMEA, 37 rows, every row naming a proving test) — ✅ 2026-07-29
- [x] TASK-0063: Author slo.md (SLI definitions, TBD-until-measured targets, error budget policy) — ✅ 2026-07-29
- [x] TASK-0064: Author disaster-recovery.md (backup inventory, executable restore drill, RPO/RTO TBD-until-measured) — ✅ 2026-07-29
- [x] TASK-0065: Author benchmarks.md as the append-only benchmark ledger with entry schema and rules — ✅ 2026-07-29
- [x] TASK-0066: Weave Phase 2 surfaces into references (RFC 9457 error model in api-reference; doctor/estimate/replay/benchmark/export/import in cli-reference; D1–D12 + reliability keys in config-reference; full pyramid in testing-strategy; supply chain in security) — ✅ 2026-07-29
- [x] TASK-0067: Update CLAUDE.md with Phase 2 permanent constraints (provable claims, error taxonomy, flags-default-false) — ✅ 2026-07-29

### Gate A — audit (zero open AUDIT tasks = gate complete)

- [x] AUDIT-0001: architecture.md contained unmeasured superlative "fastest" for the fixed chunker — rephrased to a factual statement — ✅ 2026-07-29
- [x] AUDIT-0002: scope.md contained two "best-in-class" claims — rephrased as goals per the provable-claims policy — ✅ 2026-07-29
- [x] AUDIT-0003: scope.md control-plane reconciliation predated the Python package surface — updated to API + CLI + library — ✅ 2026-07-29
- [x] AUDIT-0004: CLAUDE.md pointer list was missing python-api.md — added — ✅ 2026-07-29
- [x] AUDIT-0005: structure.md directory tree was missing python-api.md — added — ✅ 2026-07-29
- [x] TASK-0068: Gate A audit complete (files/sections verified; commit log 100% single-line with zero attribution; exactly one todo file; config-reference parameter coverage verified; pain points and reconciliation verified; ADRs MADR-style and sequential) — ✅ 2026-07-29
- [x] TASK-0069: Gate B documentation hardening complete (all Section 3 files exist with required sections) — ✅ 2026-07-29
- [x] TASK-0070: Merge documentation branch to main and push — ✅ 2026-07-29

### Phase 3 — repository hardening

- [x] TASK-0088: Add .gitignore (secrets, caches, build artifacts) — ✅ 2026-07-29
- [x] TASK-0089: Add .env.example matching the security.md variable inventory — ✅ 2026-07-29
- [x] TASK-0090: Commit canonical default config.yaml, byte-consistent with the config-reference.md example — ✅ 2026-07-29
- [x] TASK-0091: Add GitHub PR template (rule-enforcement checklist) and bug/feature issue templates — ✅ 2026-07-29
- [x] TASK-0092: Add .github/SECURITY.md private vulnerability-reporting policy — ✅ 2026-07-29
- [x] TASK-0093: Author docs/glossary.md pinning canonical terminology — ✅ 2026-07-29
- [x] TASK-0094: Author docs/references.md consolidating external evidence sources (R1–R14) — ✅ 2026-07-29
- [x] TASK-0095: Author docs/archive-format.md specifying the D11 portability archive (v1.0.0) — ✅ 2026-07-29
- [x] TASK-0096: Add the golden-set JSONL schema to testing-strategy.md (shared by eval harness, D6, D7) — ✅ 2026-07-29
- [x] TASK-0097: Merge repo-hardening branch to main and push — ✅ 2026-07-29

### Phase 4 — adoption guides and build-de-risking specs

- [x] TASK-0099: Documentation consistency audit (relative links, config-key drift vs config.yaml, error codes, metric names, CLI commands) — zero real defects found — ✅ 2026-07-29
- [x] TASK-0100: Author quickstart.md (zero-to-answered-query for CLI, Python, and REST paths) — ✅ 2026-07-29
- [x] TASK-0101: Author data-model.md (canonical entity schemas, ID scheme, cross-entity invariants) — ✅ 2026-07-29
- [x] TASK-0102: Author prompts.md (P1 generation, P2 contextual enrichment, P3 faithfulness, P4 golden-set contracts) — ✅ 2026-07-29
- [x] TASK-0103: Author troubleshooting.md (symptom → cause → fix, user-facing inverse of the FMEA) — ✅ 2026-07-29
- [x] TASK-0104: Author cookbook.md (nine composable configuration recipes) — ✅ 2026-07-29
- [x] TASK-0105: Author migration-guide.md (concept mapping, migration procedure, honest trade-offs) — ✅ 2026-07-29
- [x] TASK-0106: Cross-link Phase 4 docs from README, CLAUDE.md, and structure.md — ✅ 2026-07-29
- [x] TASK-0107: Merge adoption-guides branch to main and push — ✅ 2026-07-29

### Slice S1 — skeleton (build phase)

- [x] TASK-0021: Scaffold repository per structure.md (pyproject, src layout, tooling: ruff, mypy strict, pytest, pre-commit) — ✅ 2026-07-29
- [x] TASK-0072: Implement error-taxonomy base classes, RFC 9457 problem responses, structured logging with correlation ids — ✅ 2026-07-29
- [x] TASK-0022: Implement config loader (pydantic-settings + YAML source, .env secrets, fail-fast validation incl. all cross-field rules) — ✅ 2026-07-29
- [x] TASK-0039: Implement REST API app factory + routers (endpoints land incrementally per slice; /healthz + /readyz first) — ✅ 2026-07-29

### Slice S2 — Qdrant adapter + doctor v1 (build phase)

- [x] TASK-0023: Implement VectorDBAdapter base interface + factory (create_collection, upsert, search, update, delete, health) — ✅ 2026-07-30
- [x] TASK-0024: Implement Qdrant reference adapter (docker | external | remote modes, 6333/6334 both handled, prefer_grpc, API key) — ✅ 2026-07-30
- [x] TASK-0025: Implement Qdrant system-managed Docker provisioning (named volume enforced on Windows/WSL, both ports exposed) — ✅ 2026-07-30
- [x] TASK-0073: Implement the shared adapter contract test suite (run against Qdrant in all three modes) — ✅ 2026-07-30
- [x] TASK-0074: Implement fasterrag doctor v1 (Docker, ports, disk, RAM/GPU, DB reachability all modes, key validity, config validity; fix-it strings; provisioning gate) — ✅ 2026-07-30

### Slice S3 — ingestion core (build phase, in progress)

- [x] TASK-0110: Register the vector database health check with `/readyz` — ✅ 2026-07-30
- [x] TASK-0028: Implement parsing pipeline (PDF incl. tables/OCR, HTML, Markdown, DOCX, TXT, CSV/JSON; golden-file tests) — ✅ 2026-07-30
- [x] TASK-0029: Implement chunking pipeline (recursive baseline first; then fixed, semantic, layout, late; Hypothesis invariants) — ✅ 2026-07-30
- [x] TASK-0026: Implement embedding provider adapters (HuggingFace local first; OpenAI, Cohere, Ollama) + tiering router — ✅ 2026-07-30
- [x] TASK-0075: Implement checkpointed journal, content-hash dedup, DLQ with reason codes, per-document status API (D3) — journal, dedup, DLQ, and the query methods; the REST endpoints land with the ingest router — ✅ 2026-07-30
- [x] TASK-0031: Implement CPU worker pool (load/parse/chunk) with bounded queue hand-off and backpressure — ✅ 2026-07-30
- [x] TASK-0032: Implement stateful embedding worker pool (model loaded once per worker, batched, retryable) — ✅ 2026-07-30
- [x] TASK-0117: Decide the sparse-retrieval design before the indexer lands — resolved as ADR-0007: BM25 lives in the vector database as sparse vectors, with term encoding in fasterRag and IDF delegated to the backend — ✅ 2026-07-30
- [x] TASK-0033: Implement indexer (batch dense upsert + BM25 index + metadata, deterministic chunk IDs) — ✅ 2026-07-30
- [x] TASK-0118: Wire the two pools into an ingestion service that owns the job lifecycle — create the job, run both pools concurrently against one bounded queue, checkpoint as documents complete, and mark the job completed, failed, or partial — ✅ 2026-07-30
- [x] TASK-0076: Implement fasterrag estimate / POST /v1/estimate preflight cost estimator (D9) — the estimation service; the CLI and REST surfaces land with their own slices — ✅ 2026-07-30

### Slice S4 — retrieval (build phase)

- [x] TASK-0034: Implement hybrid retrieval (dense + BM25 parallel legs), RRF fusion (k=60), metadata filter push-down — ✅ 2026-07-30

### Slice S5 — rerank and eval harness (build phase, in progress)

- [x] TASK-0047: Implement eval harness (recall@k, MRR, nDCG, faithfulness) with dataset fixtures — retrieval metrics and the golden-set format; faithfulness needs the LLM call site and lands with the generation slice — ✅ 2026-07-30
- [x] TASK-0035: Implement config-gated cross-encoder reranker stage (top 100–1000 → rerank → top_k) — ✅ 2026-07-30
- [x] TASK-0121: Verify the embedder and reranker against real downloaded models — `tests/eval/test_real_models.py`, marked `eval`, wired into CI as its own job; it found and fixed a sentence-transformers 5.x accessor deprecation — ✅ 2026-07-30
- [x] TASK-0122: Implement the retrieval regression gate (D7) — tolerance comparison against a committed baseline, refusing a baseline recorded under a different embedding model or retrieval configuration, and blocking rather than passing when none exists — ✅ 2026-07-30

### Slice S6 — generation (build phase, in progress)

- [x] TASK-0036: Implement context assembly (token budgeter, dedup, span-level citations) — ✅ 2026-07-30
- [x] TASK-0027: Implement LLM provider adapters (OpenAI, Anthropic, Cohere, Ollama, OpenAI-compatible; streaming) — ✅ 2026-07-30

### Workflow

- [x] TASK-0108: Land slice S1 on main — ✅ 2026-07-30
- [x] TASK-0109: Land slice S2 on main — ✅ 2026-07-30
- [x] TASK-0112: Adopt trunk-based development on main and update CLAUDE.md, CONTRIBUTING.md, and the revert playbook (maintainer instruction) — ✅ 2026-07-30

### First formal audit (2026-08-02)

- [x] TASK-0169: Execute the first formal production-grade audit — gates re-run green (ruff, format, mypy strict over 216 files, 1224 unit tests, 87% branch coverage on the gated packages); progress scored 68/100 and implementation-vs-requirements 71/100; findings filed as TASK-0155–TASK-0168 plus two maintainer-decision items (TASK-0164/0165); stale-state corrections applied across CLAUDE.md, README, reliability, api-reference, glossary, failure-modes, quickstart, cookbook, python-api, integrations, data-model, deployment, and CONTRIBUTING; AUDIT-0006/0007 resolved; ledger hygiene repaired (duplicate S2 block removed, duplicated TASK-0110 renumbered to TASK-0155) — ✅ 2026-08-02

### Documentation completion pass (2026-08-10)

- [x] TASK-0254: Author the reproduction-and-operation documentation layer, closing the gap between "the docs specify the system" and "the docs suffice to rebuild, verify, release, and use it": `docs/internals.md` (spec→module→proof map over the real tree, lifecycle traces, design invariants, extension recipes each naming the gate that catches mistakes, maintainer gotchas), `docs/rebuild-playbook.md` (the S1–S14 build order with load-bearing dependencies and the 22-entry trap registry harvested from what actually went wrong), `docs/verification.md` (tiered acceptance procedure from static gates to live capability walkthrough, with §7 as a read-only cross-reference of open verification citing existing TASK ids only — todo.md remains the single tracker), `docs/release.md` (full release procedure for the `fasterrag` PyPI package with the authorization threshold: confidence ≥ 85 re-derived within 7 days + zero open release-gating items + CI green on the tag commit; ≥ 90 plus the completed measurement program for 1.0), and `docs/how-to-use.md` (the post-release user manual, banner-noted until the first release makes it literal). All five wired into CLAUDE.md, README, and structure.md — ✅ 2026-08-10

### Release-readiness review (2026-08-10)

- [x] TASK-0253: Execute the release-readiness review and publish the assessment as `docs/release-readiness.md` — gates re-verified on the working tree (2,060 fast tests green in 103 s, ruff clean, mypy strict clean over 308 files); every capability the README claims shipped was independently probed and matched, confirming the ledger is trustworthy; release confidence scored **62/100 for tagging `v0.1.0-beta.1` today**, rising to ~84/100 on completion of the release-gating checklist (§3.1 of the assessment), which contains only decisions and single-boundary fixes — no hardware, no new subsystems. Two findings the ledger did not hold were filed: the REST/CLI provisioning asymmetry (TASK-0251) and the coverage gate measuring 83% on the gated packages against its 85 threshold on this machine (TASK-0252); the stale 87.11% figure in testing-strategy.md was corrected in the same change. Nothing else was ticked, edited, or reworded — ✅ 2026-08-10

## In Progress

_(empty)_

## Pending

- [ ] TASK-0020: Decide beta version number and stamp CHANGELOG Unreleased → 0.1.0-beta.1 on first release
- [ ] TASK-0098: (maintainer action, GitHub settings) Enable branch protection on main — require PRs, block direct pushes
- [ ] TASK-0111: (maintainer action) Tag the landed slice boundaries `v0.1.0-s1` and `v0.2.0-s2` on main
- [x] TASK-0155: Register the vector database health check with `/readyz` — already wired by `_vector_db_check` in `api/main.py`; the task text was stale, carried forward from the renumbering. Verified live rather than by reading: healthy returns `200` with `vector_db reachable in 17.982 ms`; with the Qdrant container stopped `/readyz` returns `503 NOT_READY` naming `vector_db` and `qdrant was unreachable during health: ResponseHandlingException`; restarting recovers to `200`. `tests/unit/api/test_health.py` already covers the failing path — ✅ 2026-08-02
- [x] AUDIT-0007: deployment.md states every provisioned container runs non-root, but the Qdrant provisioner does not pass `--user` — the official image expects to own its storage volume and forcing a uid risks an unstartable container. Decide whether to verify a working non-root uid for the pinned image or to narrow the documented claim to the containers fasterRag builds itself — resolved by narrowing the claim in deployment.md — ✅ 2026-08-02
- [x] AUDIT-0006: `trace_id` format conflict — data-model.md gives Trace ids a `t_` prefix and states the id equals the value propagated through logs, spans, and problem responses, while api-reference.md's example and the "matches OTel trace" requirement imply a bare 32-hex OpenTelemetry trace id. S1 implements 32-hex lowercase for OTel compatibility; reconcile the two documents — resolved by dropping the `t_` prefix from data-model.md, matching the implementation — ✅ 2026-08-02
- [ ] TASK-0164: (maintainer decision) Write an ADR making the license choice explicit — GPL-3.0-or-later (current) vs a permissive license, given the adoption thesis is embedding fasterRag inside other products; either answer is fine, undocumented default is not
- [ ] TASK-0165: (maintainer decision) ADR-0008 on degradation-ladder scope — implement the circuit breaker (TASK-0148) and `cache_only` rung (TASK-0159) as specified, or formally narrow D4 to the two shipped rungs; reliability.md, api-reference.md, glossary.md, and failure-modes.md carry specified-not-built annotations until this is decided

## Todo

> **Gate C authorized (2026-07-29).** The maintainer opened the build phase; slices S1–S13 have landed (see Done and the ticked entries below). Remaining work is fully specified and strictly ordered, and a new slice begins only on explicit maintainer authorization. Work lands directly on `main` (trunk-based, maintainer instruction 2026-07-30) — every commit leaves `main` green, meets the per-slice Definition of Done ([testing-strategy.md](testing-strategy.md) §4), and slice boundaries are tagged `v0.x.0-sN`. *(The duplicate unticked S2 block that previously sat here was removed 2026-08-02 — those five tasks were already ticked in Done under their same ids.)*

### S3 — Ingestion core

- [x] TASK-0030: Implement contextual enrichment (P2) — `core/chunking/enrichment.py` prepends a document-level context to each chunk before embedding and indexing. The parent document leads every prompt verbatim so it stays a cacheable prefix; without that, cost scales with document length times chunk count rather than chunk count alone. Calls run concurrently, since one round trip per chunk in sequence would make ingestion latency the sum of every chunk's call. **A failed enrichment is never fatal** — the chunk indexes unprefixed and carries `enrichment_failed`, because a slightly worse chunk beats a dead-lettered document, and an unflagged failure would be indistinguishable from the feature being off. `chunking.context_tokens` is interpolated into the prompt rather than hardcoded; a setting the prompt ignores silently does nothing. **Verified against the live provider** on a policies fixture: the model returned the section, region, year, and the referents the chunk's shorthand depends on, with `original_text` preserved and zero failures. 16 tests — ✅ 2026-08-04
- [x] TASK-0185: Wire enrichment into ingestion — `chunking.contextual_enrichment: true` now changes what is indexed. The process-boundary constraint resolved as documented: parsing stays in the process pool, the parsed text comes back on `ParseOutcome.document_text`, and the provider call happens on the async side after the executor returns — so parsing stays parallel and CPU-bound while an I/O wait sits on the event loop where it belongs. The parent text is carried **only when the toggle is on**, or every parsed document would double what it sends through IPC for a field nothing reads. The enrichment model is built only when the toggle is on, and a missing model leaves the flag inert rather than raising — a toggle cannot conjure a provider. **Verified against the live provider through the real pool**: two documents in, two enriched, originals preserved. 4 wiring tests on top of the 16 unit tests. `fasterrag estimate` does not yet account for the extra calls — TASK-0186 — ✅ 2026-08-04
- [x] TASK-0186: `fasterrag estimate` accounts for contextual-enrichment calls — reported as its own block rather than blended into the embedding figure, because enrichment is a generation charge on a different model at different rates and one combined number hides which knob to turn. The prompt cost is document tokens × chunk count, which is the term that makes it expensive and the term a per-chunk figure would conceal. Quoted **uncached** and the basis string says so: prompt caching is what makes enrichment affordable, but its discount depends on the provider and cache window, so a number fasterRag cannot verify would understate a real bill. An unpriced model reports `unknown`, never zero — zero reads as free. Verified on the policies corpus: 12 calls, $0.0011, against $0.0000 for local embedding. 8 tests, including one asserting the estimator loads no tokenizer — a preflight that pulls a multi-gigabyte model is not a cheap preflight — ✅ 2026-08-04
- [x] TASK-0113: Implement the pooling half of late chunking in the embedding pool (embed the document in one long-context pass, then pool token representations over each chunk's span); the boundary half and the `late_pooling` marker already ship — ✅ 2026-08-05 (`core/chunking/pooling.py` runs the model over the whole document and mean-pools the token states inside each chunk's span. Verified against the real local `BAAI/bge-small-en-v1.5`: on a three-sentence document whose last chunk reads "It was raised from 35 pounds", the query "what was the UK meal allowance raised from" retrieves the *wrong* chunk with ordinary embedding and the right one with pooling — an illustration of the failure mode, not a benchmark entry. Documents beyond the model's context are processed in overlapping windows, each token taking its state from the window that gave it the most surrounding context, so a 1,440-token document on a 512-token model pools correctly instead of being truncated. A hosted provider cannot supply token-level output, so the pool falls back to ordinary embedding rather than failing — `late` is then no better than `recursive`, not broken. The document text reaches the embedding stage as one shared string object attached on the parent side, so it costs a pointer per chunk rather than a copy: measured 1 distinct object across a document's chunks. **A corpus-corrupting hazard was found while wiring it** — pooling regroups a batch by document, so writing the original batch order against pooled vectors pairs every chunk with another chunk's vector, producing a corpus that indexes cleanly and retrieves nonsense; the write now follows the regrouped order and a test fails if that is reverted)
- [x] TASK-0114: Replace the estimating token counter with the embedding provider's real tokenizer once an embedding adapter is configured, so `chunking.chunk_size` counts true tokens rather than a four-characters-per-token estimate — ✅ 2026-08-04 (measured against the local `BAAI/bge-small-en-v1.5` tokenizer: the estimate undercounted Japanese by 4.3× and code by 1.9×, so a `chunk_size: 256` chunk of Japanese was really 1,092 tokens and was silently truncated at the model's 512 limit. Two defects were found while wiring it: the counter was only reporting `token_count` and never constrained the split, and `overlap` was applied purely in characters. Both now enforce a real token budget — every chunk in the three-corpus check fits the model limit, and English prose chunks byte-identically to before. Exposed as `chunking.token_counter` and `chunking.chars_per_token`)
- [x] TASK-0188: Expand directory arguments so `fasterrag ingest <dir>` indexes the files inside it — ✅ 2026-08-05 (found while verifying `--set` end to end: `--recursive` was declared in the parser and read nowhere, and a directory source became one unreadable document, so `ingest ./corpus` reported "0 chunks from 0 documents" and exited 0. The most obvious way to use the CLI silently indexed nothing. Expansion happens at the CLI boundary rather than in the service, so the API is not handed a server-side directory walk; hidden files and `.git`/`node_modules`/virtualenvs are skipped, and files are sorted because document ids derive from job order)
- [x] TASK-0187: Add a global `--set dotted.key=value` so any setting can be overridden per invocation — ✅ 2026-08-05 (merged into the raw mapping *before* validation, so an override is held to the same range, type, unknown-key, and cross-field rules a file value is — assigning onto a built `Settings` would have skipped exactly the checks that catch a bad combination. Every command routes through one `settings_from(args)` helper, because a command calling `load_settings` directly would accept the flag and silently ignore it)
- [x] TASK-0186: Refuse `embeddings.dimensions` when a local model cannot produce that width — ✅ 2026-08-05 (reproduced: `dimensions: 256` with `BAAI/bge-small-en-v1.5` made the adapter declare 256 while emitting 384, so the collection was created at the wrong width and every upsert failed later with nothing naming the setting. Now refused at load time with both sizes in the message. Truncation is deliberately not offered — it is only lossless for a Matryoshka-trained model, and hosted providers that support it already do it server-side)
- [x] TASK-0185: Expose the retrieval, chunking, and cache tuning constants that were hardcoded — ✅ 2026-08-04 (`retrieval.bm25_k1`/`bm25_b`, `chunking.semantic_percentile`, `cache.max_entries`, `embeddings.cache.max_entries`. Each has a test asserting the value reaches its consumer, because a setting that validates but is never read reads as a supported control and silently does nothing. The BM25 pair is documented as requiring a reindex: it changes the stored sparse vectors, not just query-time scoring)
- [x] TASK-0218: Expose the parsing constants TASK-0185 left hardcoded — ✅ 2026-08-09 (a new `parsing` section: `minimum_chars_per_page` (the OCR trigger), `ocr_resolution`, `heading_size_ratio`, `max_heading_chars`, `rows_per_block`. The first two are why this exists — a corpus of scans needs a different trigger and a different render than a born-digital one, and tuning either meant editing `core/parsing`. Configuration reaches the parsers as a frozen `ParsingOptions` value object built by `create_parsing_options(settings)`, the same shape `create_chunker` uses: the factory reads the schema at the boundary and the component never sees `Settings`, which is what keeps `fasterrag.parsing` usable standalone with no `config.yaml` at all. The options are derived *inside* the worker from settings that already cross the process boundary, so nothing extra is pickled per document. Dispatch splits into two tables — the five formats with nothing to tune keep their two-argument signature rather than carrying a dead parameter that implies a knob they do not have. Every setting has a test asserting it changes what the parser does, plus one asserting the values survive the settings → worker → parser path, plus one asserting `ParsingOptions()` still equals the schema defaults so no behaviour moved. **Deliberately left hardcoded**: `_MAX_HEADING_LEVEL` (6 is the format's own ceiling in Markdown, DOCX, and HTML, not a preference), `_SNIFF_BYTES` (how much of a CSV the dialect sniffer reads — an implementation detail of dialect detection with no corpus-level meaning), and the HTML dropped-element and content-element sets (format semantics; a configurable "is `<nav>` content" is a parser rewrite, not a knob). One consequence documented rather than fixed: parsing settings are not part of `chunker_config_hash`, so changing one leaves chunk ids and the content hash untouched and a re-ingest of an unchanged corpus is deduplicated — config-reference.md says so and points at `index reembed`; folding them into the hash would renumber every chunk in every existing collection)
- [x] TASK-0184: Add `fasterrag config show` so every configurable setting, its effective value, and its default are discoverable from the CLI — ✅ 2026-08-04 (walks the schema rather than a hand-written list, so a new field needs no registration; `--changed` lists only overrides. Loads with `require_env=False` because it is most useful on the installation `config validate` refuses)
- [ ] TASK-0115: (maintainer decision) `sentence-transformers` ships as the `huggingface` extra rather than in the core install, because it pulls a multi-gigabyte deep-learning runtime that a hosted-provider deployment should not have to download. `docs/python-api.md` was updated to match. Reverting to core is a one-line change in `pyproject.toml` if the original packaging promise should stand
- [ ] TASK-0119: Rebuilding a collection is the only way to add a keyword leg — Qdrant cannot attach a sparse vector to an existing dense-only collection, and the two layouts (unnamed dense versus named dense plus named sparse) are not interchangeable. Flipping `retrieval.hybrid` on an indexed corpus therefore needs the D2 blue/green reindex path, and until that exists the adapter refuses the mismatch rather than dropping the sparse vector silently
- [x] TASK-0116: Expose the per-document status endpoints over REST (`GET /v1/ingest/{job_id}`, `GET /v1/ingest/{job_id}/documents?status=…`, `POST /v1/ingest/{job_id}/retry-dlq`); the journal already answers these queries — ✅ 2026-08-01
- [x] TASK-0128: Implement the ingest, query, retrieve, collections, and admin routers, completing the REST half of the control plane — ✅ 2026-08-01
- [x] TASK-0129: `POST /v1/ingest` accepts only `path` sources; `url` and `inline` are documented in api-reference.md but rejected with `VALIDATION_FAILED` naming what is supported, because no loader fetches them yet. Ships with the source loader — shipped as `services/sources.py`: urls are fetched with the size limit enforced both on the declared length and while streaming (a header a peer controls is not a limit), inline payloads decode base64-first with UTF-8 fallback, and both stage to a per-job temp directory that is removed when the job settles. The canonical URI — not the temp path — remains the document's identity, so re-ingesting the same url or snippet still dedups; a bare string with an http(s) scheme classifies as a url, so `fasterrag ingest <url>` works as the CLI documents; `httpx` moved from the dev extra to core dependencies since the loader imports it at runtime — ✅ 2026-08-03
- [ ] TASK-0130: Ingestion runs as an in-process background task rather than through a queue backend. The documented contract (`202` + job id, poll `GET /v1/ingest/{job_id}`) is satisfied identically by a distributed queue, so moving to one changes no response — but a restart mid-job currently relies on journal resume rather than queue redelivery. Maintainer decision on the queue backend

### S5 — Rerank + eval harness v1

- [x] TASK-0077: Implement the golden-set generator (P4) — stratified sampling, adversarial records, `source: autopilot` provenance; verified against a real 21-chunk PDF corpus producing 10/10 records with 2 adversarial and 0 dropped — ✅ 2026-08-01
- [x] TASK-0139: Commit a golden set and a measured baseline for a fixture corpus under `tests/eval/datasets/` — the `handbook` fixture: 6 documents, 15 hand-authored records (12 answerable, 3 adversarial), document-level ground truth, and a baseline measured against a real Qdrant at recall@5 1.0 / MRR 1.0 / nDCG@5 1.0 — ✅ 2026-08-01
- [x] TASK-0141: On Windows a document id hashes the source path string, so the same file ingested as `d:\...` and `D:\...` gets two different document ids. Content-hash dedup still prevents a double index, but `index.lock` document hashes and drift detection would treat them as different documents. Found while building the eval baseline, where a resolved-vs-globbed path produced a recall of 0.0 that looked like a retrieval failure. Consider normalising the source URI before hashing, platform-aware — `normalise_source` folds Windows paths (drive letter, separator, and case) before hashing, so `d:\docs.md`, `D:\docs.md`, and `D:/docs/a.md` are one document. URLs are left untouched: their paths are case-sensitive by specification, and folding one would merge two genuinely different resources. POSIX paths keep their case for the same reason — ✅ 2026-08-03
- [x] TASK-0142: The handbook baseline detects large regressions but not small ones. Verified to catch an index missing four of six documents (recall 1.0 → 0.3333, gate blocked, exit 5), but at six documents and k=5 a retriever returns five of six, so a *subtle* ranking regression would pass. A discriminating baseline needs a larger fixture with topically overlapping documents — added `tests/eval/datasets/policies`: 12 documents in three families of four near-identical siblings (region x year, and role), so each query's answer must beat three look-alikes rather than five unrelated documents. Measured baseline **recall@5 1.0, MRR 0.875, nDCG@5 0.9077** — recall saturates while ranking keeps headroom, which is exactly what the handbook fixture lacks. Proven against a real regression: disabling the BM25 leg moves the new fixture to MRR 0.8125 / nDCG 0.8616 (−0.046, past the 0.02 tolerance) while the handbook fixture stays at a flat 1.0/1.0 and sees nothing — ✅ 2026-08-03
- [x] TASK-0143: Dropping a collection without clearing the journal makes a re-ingest a correct no-op — the content hashes are remembered per collection, so nothing is re-indexed and the collection is never recreated. That is D3 behaving as designed, but it is a sharp edge during recovery; `disaster-recovery.md` §4 documents restore-from-snapshot as the path, and should say explicitly that re-ingest is not an alternative unless the journal is cleared too — `disaster-recovery.md` §4 now carries an **Index lost, journal intact** row stating that re-ingest is not a recovery path, plus a "re-ingest trap" subsection explaining why deduplication cannot distinguish "already indexed" from "indexed into an index that no longer exists", what the `deduplicated` count looks like when it happens, and the two supported ways out — ✅ 2026-08-03
- [x] TASK-0140: Expose golden-set generation on the CLI (`fasterrag autopilot generate-golden-set` or similar). Today it is a library call only, so the shared machinery Autopilot (D6) depends on is reachable from Python but not from the terminal — `fasterrag autopilot generate-golden-set <sources...>` parses and chunks the corpus exactly as ingestion would, so no collection is needed and none is read; a golden set is usually wanted before the index exists. Refuses to overwrite an existing file, since a generated set is hand-curated afterwards. Verified live against the handbook corpus: 6 records, 1 adversarial, 0 dropped, with sensible questions — ✅ 2026-08-03
- [x] TASK-0120: Add the faithfulness metric to the eval harness once the generation slice provides the P3 call site; the harness reports retrieval metrics only until then — `score_faithfulness` answers every answerable golden query and grades it with P3, reporting the mean plus counts of refused and ungraded. Kept as a separate entry point, never called by retrieval scoring: a hundred-record set is two hundred provider calls, and a metrics run that silently started generating would bill for itself. Adversarial records are excluded — they are deliberately unanswerable, so grading one would score the system down for correctly refusing — and refusals are counted rather than folded in as zeros — ✅ 2026-08-03

### S6 — Generation

- [x] TASK-0037: Implement generation service with SSE streaming (meta/token/citations/usage/done/error events) — ✅ 2026-07-31
- [x] TASK-0078: Implement grounded-or-refuse mode with faithfulness scoring and INSUFFICIENT_EVIDENCE responses (D5) — ✅ 2026-07-31
- [ ] TASK-0123: Maintainer review — no doc specified how grounded-or-refuse behaves while streaming, and refusing after tokens have been sent is impossible. Implemented as: with `generation.grounded_or_refuse: true` the stream buffers the answer, grades it, then emits either the whole answer or one `insufficient_evidence` event followed by `done`. This trades time-to-first-token for the guarantee, only when the flag is on. Recorded in `docs/api-reference.md` §Streaming semantics; confirm or choose the alternative (stream tokens and emit a retraction event)

### S7 — Semantic cache

- [x] TASK-0038: Implement embedding cache + semantic response cache (cosine threshold, TTL + corpus-change invalidation, hit/miss metrics) — ✅ 2026-07-31
- [x] TASK-0124: Implement the `redis` cache backend behind a `.[redis]` extra. `embeddings.cache.backend` and `cache.backend` both accept `redis` per config-reference.md, but only `memory` and `disk` are implemented; selecting `redis` raises a `ConfigError` naming the alternatives rather than silently falling back. Needs a Docker Redis for the integration test — `RedisStore` ships in `core/cache/redis.py`, wired through `_create` and reached by `embeddings.cache.redis_url` / `cache.redis_url` (new settings, both defaulting to `redis://localhost:6379/0`). The `ConfigError` is replaced, not bypassed: it now fires only when the optional client is absent, naming `pip install fasterrag[redis]`, and the client is imported lazily inside the constructor so nothing else pays for the dependency. **The entry ceiling is enforced by a sorted-set index rather than left to Redis**, because `maxmemory-policy` is server-wide, needs server configuration we do not own, and would evict a co-tenant's keys as readily as ours — while counting our own by scanning the keyspace is O(everything stored). Reads restack the score, so eviction is least-recently-*used*, matching `MemoryStore`. The index is also what `items` walks and what `clear` deletes: `clear` is never `FLUSHDB`, so a corpus change cannot wipe a neighbour's data, and the two caches take separate namespaces (`fasterrag:embedding`, `fasterrag:semantic`) so one server can back both. Index is written before the value on purpose — interrupted the other way round it would leave a value no index knows about, invisible to the ceiling, to `items`, and to `clear`, which is a leak rather than a miss. TTL is written twice, into the payload (authoritative, so an expired entry is a miss on every backend) and as the key's native expiry (so Redis reclaims the memory). `decode_responses` is forced off: values are packed float32 behind a struct header, and a decoding client turns every one into a permanent miss. Every failure is translated to `CacheError` at the boundary and the message never echoes the URL, which can carry a password. 36 unit tests against a fake client plus 9 integration tests against a real `redis:7` container on port 6399 (a distinct name and port, so a developer's own Redis is never adopted or removed) — ✅ 2026-08-09

- [x] TASK-0125: The error-code table has no code for "the vector database rejected our credentials". The Qdrant adapter currently reports `EMBED_PROVIDER_ERROR`, documented as "Embedding provider timeout / hard failure", so a vector-DB auth failure surfaces under an embedding label — misleading when grepping logs. `AUTH_INVALID` is reserved for fasterRag's own API auth and `RETRIEVAL_FAILED` is documented retryable, which this is not. Add a `VECTOR_DB_AUTH` code to `errors.py` and `api-reference.md`, then use it in `_auth_error`. Found by running `fasterrag index list` against a key-protected Qdrant, 2026-07-31 — added `VECTOR_DB_AUTH_FAILED` (503, non-retryable). Sending an operator to check their embedding provider over a rejected Qdrant key is a wrong answer with a confident tone; the taxonomy exists to point at the right knob — ✅ 2026-08-03

### S8 — CLI complete

- [x] TASK-0040: Implement CLI (serve, worker, ingest, query, index, provision, status, doctor, estimate, config validate) — `replay`, `benchmark`, `export`, `import`, and `autopilot` are registered but report the slice that ships them, since their services do not exist yet — ✅ 2026-07-31
- [ ] TASK-0126: Maintainer review — `VectorDBAdapter` gained `list_collections()` and `drop_collection()` under TASK-0040, with a vendor-neutral `CollectionInfo` result. `adapters/` is a folder-boundary-sensitive package; the addition is judged to strengthen the boundary rather than weaken it, because the documented `fasterrag index list` and `index delete` are otherwise unimplementable without the CLI reaching into a vendor client directly. Both are covered by the shared contract suite. Confirm or revert
- [x] TASK-0127: The semantic cache is unusable from the CLI: `cache.backend` accepts only `memory` and `redis`, and a memory cache dies with each short-lived CLI process, so no two `fasterrag query` invocations can ever share one. Verified in-process instead (16.7s cold vs 69ms on a hit against a real corpus). TASK-0124's redis backend resolves it; until then consider documenting in cookbook.md that the semantic cache benefits `fasterrag serve` and the library, not one-shot CLI calls — `cache.backend` now accepts `disk`, the only backend that survives between two invocations of a command-line tool. `memory` remains the default for the server, and `redis` still fails fast until TASK-0124 — ✅ 2026-08-03
- [ ] TASK-0216: The per-key rate limiter is an in-process fixed window, so N replicas grant N times the configured limit. `security.md` and `api/auth.py` both say so rather than claiming otherwise, but the citation they carried pointed at TASK-0124 as the shared state that would fix it. That shared state now exists (the `redis` cache backend), so the honest statement is that the limiter is not wired to it. Either wire it — a shared counter keyed per API key, degrading to the in-process window when Redis is unreachable, since a limiter that fails closed takes the API down with the cache — or record the single-replica limit as a deliberate boundary in `slo.md`

- [ ] TASK-0079: Implement export/import portability archives + vector-copy and re-embed migration paths (D11) — **in progress**. The read side landed 2026-08-03: `VectorDBAdapter.iterate_points` walks a whole collection through Qdrant's scroll cursor, returning vendor-neutral `Point`s with the original fasterRag id restored from the payload and named dense vectors unwrapped. An async iterator rather than a list, because a collection is precisely the thing that does not fit in memory. 10 tests cover the cursor loop, multi-page streaming, exhaustion, both vector layouts, and error translation — but against a fake client only: **Docker was down, so nothing has iterated a live collection yet** (same blocker as TASK-0178). The **archive writer** landed next: `export_archive` streams rows through a staging directory rather than assembling them in memory — a collection is exactly the thing that does not fit — and writes the manifest last because it carries counts that only exist once the rows do. Members get a fixed mtime and owner so two exports of an unchanged collection are byte-identical, or every backup diff reports a timestamp and hides the real changes. Documents are reconstructed from chunk payloads rather than read from the journal, because the journal describes an ingestion *run* while the archive must describe the collection as it stands. An empty collection refuses to export: an empty archive imports cleanly and silently produces an empty collection. 15 tests. The **reader and both import paths** landed next: `open_archive` verifies format major, required members, checksums, manifest counts, and referential integrity *before* a caller can write anything — a half-imported collection looks like a corpus rather than an error, so the ordering is the safety property. It also refuses any tar member that is not a plain file at the archive root, rejecting the classic traversal at the boundary. A vector copy is permitted only when the archive carries vectors *and* model, model version, and dimensions all match, with every mismatch named rather than summarised. `--reembed` is always legal because it derives what it stores. `export` and `import` are now real CLI commands rather than `PENDING_COMMANDS` entries. 38 tests across writer and reader; `archive.py` at 100% coverage, `archive_import.py` at 91%. **Still unverified against a live backend** — no archive has been exported from or imported into a real Qdrant, and the D11 round-trip acceptance test cannot run until Docker is back (B7.2). `POST /v1/admin/export` and `/import` are now served, tenant-scoped, and covered by a round-trip test that exports and re-imports entirely over REST. **A real defect surfaced there**: a deployment with no lockfile and no configured `embeddings.dimensions` recorded `dimensions: 0` in the manifest, and an archive declaring zero dimensions cannot be imported at all — the export succeeded and produced something unusable. Both sides now fall back to the width actually observed in the data. The reverse-drift half of the OpenAPI gate earned its keep here too: it caught `api-reference.md` still marking both endpoints **Not yet implemented** after they shipped. **What remains is verification, not construction**: no archive has round-tripped through a real Qdrant, and the D11 acceptance test (Qdrant → Qdrant vector copy, Qdrant → pgvector re-embed) needs both a running daemon (B7.2) and the pgvector adapter (TASK-0049)
- [x] TASK-0183: CI went red on the Linux leg with four `NotADirectoryError`s that pass on Windows. Not a test artifact: three degradation paths (`TraceStore.store`, `DiskStore.set`, `LockStore.write`) called `temporary.unlink(missing_ok=True)` from inside their own `except OSError` handler. When the parent is a file rather than a directory, Linux raises `NotADirectoryError` *from the cleanup*, replacing the error being handled with a second one that escapes — so a path designed to degrade crashed instead. Windows does not raise there, which is exactly why only the Linux leg caught it. Each cleanup is now wrapped in `suppress(OSError)`. Reproduced locally before fixing and confirmed the store degrades rather than raising — ✅ 2026-08-03
- [ ] TASK-0182: (maintainer review) `VectorDBAdapter` gained `iterate_points` under TASK-0079. It is the read side of portability and every future adapter must implement it — Qdrant has `scroll`, pgvector would be a cursor, but a backend with no scan primitive could not satisfy the contract at all. Same review as TASK-0126 and TASK-0133: confirm the addition before it hardens
- [x] TASK-0046: Implement security layer — **API-key auth with scopes and per-key rate limiting shipped**; `security.auth` has left the accepted-but-unenforced list. Enforced as ASGI middleware rather than per-route dependencies, so forgetting to protect a new router is impossible by construction and an unmapped path requires `admin` rather than defaulting to open. Auth runs before metrics so a rejected request cannot inflate counters. `AUTH_MISSING`/`AUTH_INVALID`/`AUTH_SCOPE`/`RATE_LIMITED` are RFC 9457 problems, an unknown key is indistinguishable from a revoked one, keys compare with `secrets.compare_digest` and no early exit, and enabling auth without a usable key refuses to start. 24 tests. **Multi-tenancy isolation is NOT included** and `security.multi_tenancy` still refuses to start — that half is TASK-0179 — ✅ 2026-08-03
- [x] TASK-0179: Multi-tenancy isolation — the tenant is resolved once in `AuthMiddleware` from the key and the configured header, refusing a mismatch with `TENANT_FORBIDDEN` and refusing a missing header outright, since an unstated tenant would silently act on the untenanted namespace. A key with no tenant is an operator credential that may act as any tenant but must still name one, and a refusal never echoes the key's own tenant so tenants cannot be enumerated from the error. The **semantic cache is scoped on the entry, not the key**: lookup compares vectors across every stored entry, so a key prefix alone would not stop a similar question from one tenant returning another's answer — the owning tenant travels inside the entry and a mismatch is skipped. The tenant is in the key as well, or two tenants asking the same question collide and the second overwrites the first. `security.multi_tenancy` now starts. 11 tests. Collections, traces, and metric labels are not yet scoped — TASK-0180 — ✅ 2026-08-03
- [x] TASK-0180: Finish tenant-tagging the remaining state — **collections, traces, and metric labels**. Collections are scoped by *name* rather than by a filter, because a vector database has no notion of our tenants: a `<tenant>__` prefix makes isolation a property of the identifier, so a missing check fails closed instead of open. The prefix never reaches a response, and a tenant id containing the separator is refused — `acme__x` could otherwise address `acme`'s collection `x`. Another tenant's trace or collection is reported **absent, not forbidden**, since a distinct error confirms the id is real and trace ids are guessable from a caller's own listing. Metric series take the tenant the middleware resolved rather than one re-read from the header, so an unauthenticated request cannot label its own series. 24 tests. Ingestion jobs and the journal are still unscoped — TASK-0181 — ✅ 2026-08-03
- [x] TASK-0181: Scope ingestion jobs and the journal to their tenant — `load_job(job_id, tenant=...)` raises the same `NOT_FOUND` for another tenant's job as for an unknown id, with an identical message, because a job record carries the source paths of the corpus and job ids sort chronologically, so a caller holding one of their own can guess at neighbours. **Found while wiring it: `start_ingest` never recorded the tenant at all** — scoping the read path alone would have refused every tenant access to the job it had just started, presenting as a total outage rather than as isolation. Creation now records it. 6 tests, including one asserting the refusal is byte-identical to an unknown id — ✅ 2026-08-03

### S9 — Trace store + replay

- [x] TASK-0041: Implement OTel instrumentation (retrieval/reranker/context-assembly/generation trace types, trace-id correlation) — the four span types are recorded against one clock origin and correlated by trace id; OTLP export itself is TASK-0132 — ✅ 2026-08-01
- [x] TASK-0042: Implement metrics catalogue export (RED, per-stage latency, tokens, cost/query, cache ratio, queue/DLQ depth, breaker state) — every documented metric declared and served at `GET /metrics`; the gauges fed by the worker pools and circuit breaker are declared but only populated once those slices report into them — ✅ 2026-08-01
- [x] TASK-0080: Implement local trace store + fasterrag replay with side-by-side retrieval/answer diff (D8) — ✅ 2026-08-01
- [ ] TASK-0131: `fasterrag traces list|show` was added beyond `docs/cli-reference.md`, because a 32-hex trace id is unreachable without a way to list recent ones. Documented in cli-reference.md; confirm the addition
- [x] TASK-0132: OTel export is not wired. The four RAG spans are recorded and persisted by the trace store, which is what replay and the dashboard read, but `observability.otel: true` does not yet emit them over OTLP to `observability.otel_endpoint`. Needs the opentelemetry SDK dependency and an exporter — ✅ 2026-08-05 (`observability/otel_export.py` maps a `Trace` onto a root span with a child per stage and ships it over OTLP/HTTP. **The fasterRag trace id is preserved** — it is already 32 hex characters, exactly the OpenTelemetry shape, so the id in a `problem+json` body, the logs, `GET /v1/traces/{id}`, and a trace viewer's search box are one id; minting a separate one would leave an operator holding a failing request's id with no way to find it. Span ids are derived from the trace id and stage name so a retried export is one trace rather than two overlapping copies. Verified beyond the mapping: the spans encode as real OTLP protobuf, and a local HTTP receiver confirmed the full wire path — a POST to `/v1/traces` with `application/x-protobuf`, trace id intact on the wire, stage durations exact. Ships as the optional `otel` extra; a missing SDK warns rather than refusing to serve queries, and an unreachable collector costs the record and nothing else. `TraceStore` now holds a list of exporters instead of one Langfuse slot, so Langfuse and OTLP can both be on. **Not verified against a real collector** — Jaeger or an OTel Collector needs Docker, which is blocker B7)
- [x] TASK-0189: Export metrics over OTLP as well as spans. `observability.otel: true` currently ships the four RAG spans only; the metric catalogue is still Prometheus-scrape-only via `/metrics`, so a deployment whose observability stack is OTLP-native sees fasterRag's traces but none of its counters. Needs the SDK's meter provider and a periodic reader wired to the same endpoint — ✅ 2026-08-05 (`observability/otel_metrics.py` pushes the catalogue every 60s, started and stopped in the API lifespan with a final snapshot on shutdown so the last window is not lost. **One source of truth**: `Registry.snapshot()` returns typed `Sample`s that both the OTLP payload and the Prometheus text are built from, rather than one parsing the other's output. **The mapping hazard is the histogram**: Prometheus buckets are cumulative and OTLP explicit-bucket histograms are per-bucket, so sending ours unchanged would have inflated the counts — measured 3.75× on a six-bucket example — and every percentile read off the OTLP copy would have disagreed with the same percentile read off the scrape, with the OTLP one wrong. Converted, and pinned by a test asserting per-bucket counts sum to the observation count. Verified end to end: a local receiver confirmed the wire path, and the real app with the toggle on pushed a snapshot whose `requests_total` matches the scrape exactly at the same instant. **Not verified against a real collector** — that needs Docker, blocker B7)

### S10 — Zero-downtime reindex + lockfile

- [x] TASK-0081: Implement blue/green reindexing with eval-gated atomic alias swap and rollback retention (D2) — the alias swap, retention, and rollback are complete and verified against a real Qdrant; the eval gate reports that it *could not run* rather than passing, because scoring a green build needs TASK-0077's golden-set harness — ✅ 2026-08-01
- [ ] TASK-0133: Maintainer review — `VectorDBAdapter` gained `set_alias`, `alias_target`, and `delete_alias` under TASK-0081. Aliases are the primitive zero-downtime reindexing is built on and cannot be emulated above the adapter, so the contract is the only place they can live; all three are covered by the shared contract suite, including that a swap is atomic and an alias is searchable as if it were the collection. Same boundary question as TASK-0126; confirm or revert
- [x] TASK-0134: Wire the eval gate into `index reembed` — `--dataset` scores the green collection before the swap; without it the swap still records `gate_ran: false` rather than a pass — ✅ 2026-08-02
- [x] TASK-0082: Implement index.lock writing + drift detection + `index lock verify` (D1) — ✅ 2026-08-01

### S11 — Chaos, load, soak; baselines

- [x] TASK-0083: Implement the scripted chaos suite (kill-worker, stop-Qdrant, corrupt-doc, slow-LLM, disk-full) and degradation ladder verification (D4/D12) — all five scenarios scripted and passing; observed behavior recorded in failure-modes.md — ✅ 2026-08-01
- [x] TASK-0135: The chaos suite injects the stop-Qdrant and disk-full faults at the adapter and filesystem seams rather than by stopping a real container or filling a real disk. That verifies fasterRag's response but not the operating system's reporting of those conditions; add container-stop and disk-quota variants under the `integration` marker — ✅ 2026-08-09 (`tests/chaos/test_real_faults.py`, 10 cases, `chaos` + `integration`. **Both variants are real; neither is simulated.** A throwaway Qdrant on its own name, ports, and volume is really stopped with `docker stop`, and the real journal write path runs inside a Linux container whose journal directory is a tmpfs with a hard 512k limit, filled to zero free bytes — a genuine filesystem returning genuine `ENOSPC`, not an unwritable directory. The OS-level disk variant could not be built on the Windows host itself: constraining an NTFS volume needs a mounted VHD or a disk quota and both require administrator rights a test must never demand, so the container is the honest substitute and the case runs unchanged on Linux or macOS. Delivered by salvaging an abandoned worktree — what shipped, what was disproven, and the four mutations are recorded under TASK-0233, and the one live defect it exposed under TASK-0234)
- [ ] TASK-0136: Recovery time per chaos scenario is not measured — D12's proof metric asks for it, and it needs the isolated hardware TASK-0084 is blocked on
- [x] TASK-0137: The error-code table has no timeout code for generation. Embeddings distinguish `EMBED_PROVIDER_TIMEOUT` from `EMBED_PROVIDER_ERROR`, but a generation timeout can only be reported as `GENERATION_FAILED`, so `reliability.timeouts.llm_ms` firing is indistinguishable from any other provider failure in logs and metrics. Consider `LLM_TIMEOUT`, alongside TASK-0125 — added `GENERATION_TIMEOUT` (504, retryable), mirroring the embedding split. A timeout says raise `reliability.timeouts.llm_ms` or shrink the context; a hard failure says look at the provider, and one code for both cannot say which — ✅ 2026-08-03
- [x] TASK-0048: Implement benchmark suite per performance.md (p50/p95, throughput, cache hit rate, cost/query; --ledger output) — ingest and query suites; the eval suite needs TASK-0077's golden set — ✅ 2026-08-01
- [ ] TASK-0084: Run load + soak + chaos on documented reference hardware; record first benchmark-ledger entries; replace every TBD-until-measured in slo.md — BLOCKED on isolated hardware. BENCH-0001 and BENCH-0002 are committed but explicitly marked not citable: they were taken on a developer laptop with Docker, an IDE, and other co-tenant load, failing ledger rule 5's isolation requirement. Run-to-run variance exceeded the effect being measured (p50 5.5 s vs 8.7 s across two invocations of the same commit), and cold start came out *faster* than the warmed median — both symptoms of the missing isolation. slo.md's TBDs stay TBD until superseding entries come from a quiet machine
- [ ] TASK-0085: Execute the disaster-recovery restore drill for real; record RPO/RTO from measurements — PARTIALLY EXECUTED 2026-08-01. Steps 4, 5, and 7 ran for real against a live Qdrant: a collection was snapshotted, deleted, restored, and verified to answer with an identical citation. Steps 1–3 (clean host, `.env`, doctor) and step 6 (`index lock verify`) did not run, because the restore was onto the same host — that makes it the §4 single-collection shortcut, not the §2 clean-host drill. RPO/RTO stay TBD: the measured 4,311 ms is a restore duration for one tiny collection, not `T1 − T0` from a clean host. Execution log in disaster-recovery.md
- [x] TASK-0138: Implement backup/restore for the remaining §4 shortcuts and a documented default schedule (daily snapshots retained 14 days); today `fasterrag backup` is a manual command with no cadence, so the documented default is a statement about tooling that does not exist yet — ✅ 2026-08-06 (**the defect was worse than the task described**. `back_up` wrote straight into the destination, so every run overwrote the previous manifest: a deployment running daily backups held exactly *one* recovery point, not fourteen, and would have discovered that only when the newest backup turned out to be the corrupt one. Worse, the overwritten manifests were the only record of their backend snapshots, which stayed inside the vector database orphaned and invisible, consuming disk forever. Reproduced before fixing: two runs, two snapshots taken, one referenced, one leaked. Each run now writes a timestamped set, `--retain N` (default 14) prunes older sets *and* deletes the snapshots they reference, and `restore` accepts either one set or the destination. Verified: 6 runs at retain=3 leave 3 sets, 3 snapshots deleted, and **0 orphans**. Retention counts sets rather than days, because fasterRag cannot know the cadence a scheduler was configured with. **Cadence was deliberately not built** — cron, systemd timers, and Task Scheduler already do it better and are what an operator monitors; a scheduler inside a RAG framework would be a worse one that fails silently. `docs/disaster-recovery.md` now carries a crontab line instead of claiming tooling that should not exist. Two further gaps found: the backup service **had no tests at all**, which is how the overwrite survived — 17 now cover sets, retention, orphans, corrupt manifests, and restore resolution; and `fasterrag backup`/`restore` were **entirely absent from cli-reference.md** despite the parser claiming the surface mirrors it exactly. `VectorDBAdapter` gained `delete_snapshot`, which retention cannot work without — same folder-boundary review as TASK-0126/0133/0182, filed as TASK-0191)
- [x] TASK-0192: Cover `IngestionService.run` at the unit level — ✅ 2026-08-06 (a coverage audit over the whole tree found the primary write path at **31%**: source resolution, both worker pools, journal checkpointing, dead-lettering, the lockfile, cache invalidation, and the ingestion metrics were exercised **only** by a Docker-gated integration test. With the daemon down all session that path had not executed once, and CI's unit leg never touches it — so every change routed through it this session, including the late-chunking document carry and the circuit breaker, landed without the orchestration being run. Now **92%**, with 15 tests over a faked backend: completion, dedup on replay, dead-lettering for both an unreadable source and a failed provider, checkpoint resume, cache invalidation *and* the case where nothing was indexed so the warm cache must be left alone, metrics, metadata, tenancy, and idempotency replay. The existing `RecordingAdapter` is reused rather than a second partial double written, so a change to the adapter contract breaks one place)
- [x] TASK-0193: Cover the LLM adapters' call paths — ✅ 2026-08-06 (`adapters/llm/openai.py` sat at 22%: the existing suite covered construction, the factory, and failure classification, but never called `complete` or `stream`, so request building and response parsing went unexercised — the two places a provider integration is most often wrong and the two that otherwise only fail against a live API. Now 83%, with 20 tests over a faked client: message ordering, sampling settings reaching the request, token usage carried through (the cost metric multiplies these, so a dropped count reads as a free query), a null `content` becoming an empty answer rather than crashing every consumer, truncation reported, streaming asking for usage, the usage-only final event skipped rather than yielded as a blank token, and vendor exceptions becoming typed errors on both call shapes. **Verified the tests have teeth by mutation** rather than trusting the coverage number: appending the system message *after* the user message — a real bug that providers accept silently — and yielding empty deltas each fail exactly one test, and only that test)
- [x] TASK-0194: (security) `POST /v1/replay` looked its trace up **unscoped**, so any authenticated tenant could replay another tenant's trace — ✅ 2026-08-06 (found by auditing `api/traces.py`, the one API module below 90% coverage at 44%. `GET /v1/traces/{id}` and `GET /v1/traces` both passed `tenant=` to `TraceStore.load`/`recent`; replay passed nothing, so the store applied no filter. Replay re-executes the stored question and returns a diff, so the disclosure is the other tenant's query text and retrieved chunks — the exact content the read endpoints refuse, reached through a write-shaped endpoint. **Reproduced against the running API before fixing**, with auth and multi-tenancy on and two tenant-bound keys: `evilcorp` got 404 on both GETs and reached the trace on replay. Fixed by scoping the lookup, and confirmed: `evilcorp` now gets 404 while the owner still reaches its own. 10 tests added, and the key one was verified to fail against the unfixed code rather than assumed to. **Severity depends on configuration** — it only bites a deployment running `security.auth: true` *and* `security.multi_tenancy: true`, which is not the default and is a slice that shipped recently, so exposure is likely small. No other unscoped lookup exists: the dashboard and both read endpoints were already correct)
- [x] TASK-0195: Pin every registered route's required scope as a reviewable table — ✅ 2026-08-06 (follow-on from TASK-0194. `required_scope` was tested in isolation, but nothing asserted the mapping it produces for the routes the application *actually registers*. An unmapped route inherits `admin` by omission, which is the safe direction; the unsafe ones are a route landing under an existing prefix and silently inheriting that prefix's weaker scope, or a path being added to `PUBLIC_PATHS`. Neither leaves any other signal in a diff. The full inventory was audited first and is correct — 21 routes, three public probes, everything else scoped as intended, `/v1/estimate` on `admin` because it parses server-side paths. **Both failure directions were verified to trip the tripwire**: a new `GET /v1/query/debug` (which would inherit `query`) fails the inventory test, and moving `/metrics` into `PUBLIC_PATHS` fails two. Walking nested routers matters here — `app.routes` holds only `/openapi.json` in this FastAPI version, so a naive implementation would pass while inspecting nothing)
- [x] TASK-0198: Audit every CLI flag for one nothing reads — ✅ 2026-08-06 (generalising TASK-0188, where `--recursive` was declared, documented, and read nowhere, so directory ingestion silently indexed nothing. Comparing all 63 declared argparse destinations against every consumer found **five more in the same state**: `--show-chunks`, `--no-stream`, `--priority-class`, `--watch`, and `doctor --fix` — each accepted by argparse and documented in cli-reference.md as a working feature. Three are now implemented: `--show-chunks` prints the candidate set via `answer_with_candidates`, `--priority-class` sets the `priority_class` metadata the tiering rules already match on, and `--no-stream` means something because the CLI now *streams by default* — `run_query` only ever called `answer()`, so its own docstring claiming it streamed 'unless --no-stream was given' was false. `--json` and `--show-chunks` imply `--no-stream`, since a partial document is not a document. The two that cannot be built yet say so in `--help` **and** print a notice at the point of use, because help text is read once while the notice reaches whoever actually passed the flag. Filed as TASK-0196 (`--watch`, needs the queue-backed job path) and TASK-0197 (`doctor --fix`, needs Docker). A guard test now fails on any newly declared flag nothing reads; verified by adding one)
- [ ] TASK-0196: Implement `ingest --watch` and `index reembed --watch`. Both declare the flag and print a not-implemented notice today. The CLI runs its job inline to completion, so there is nothing to follow until ingestion moves behind the queue-backed job path (TASK-0130)
- [x] TASK-0197: Implement `doctor --fix` — ✅ 2026-08-11 (unblocked by a working Docker daemon, and every claim below was reproduced against one. **Two repairs qualify, and the bar is that a repair must be idempotent, non-destructive, and unsurprising**: `docker volume create` for the missing named storage volume (converges — an existing volume is returned untouched, so an index can never be reinitialised), and `docker start` for fasterRag's *own* stopped container. Everything else keeps its concrete fix-it line and is reported `needs human` with the reason: freeing a port means killing whatever holds it, freeing disk means deleting files fasterRag did not write, and `.env` and `config.yaml` are the operator's to author — a diagnostic that rewrote the config would destroy the intent it was run to explain. **Creating** a container is deliberately excluded even though it would make more reports go green: it pulls an image and publishes ports, which is `provision qdrant`'s job. **Every attempt is classified from a second full run of the report, never from the repair returning without an error** — trap T23, the T12 defect one layer up, since "created the volume" is not evidence the check now passes; the mutation that scores from the attempt instead turns `test_a_repair_that_did_not_take_is_reported_not_fixed` red. Starting a container needed a new failing check to hang off, so `vector_db_volume` was added: a missing volume is otherwise invisible until the container is replaced and the index is silently gone. It does **not** block provisioning (`provision qdrant` creates the volume, so gating on it would refuse the command that fixes it), and when the daemon is down it is **omitted rather than passed** — the docker check already owns that root cause, and a check reporting PASS on a probe that never ran is exactly T12. The safety guard is the `fasterrag.managed=true` label: a container holding the configured name without it belongs to somebody else and is left alone (trap T24), and because that guard is worthless if the string written at `docker run` ever drifts from the one read at `docker inspect`, an integration test pins the two together against a real daemon. Exit codes are unchanged — `0` when everything passes after the repairs, `4` when anything still fails — so `--fix` cannot launder a failed preflight into a green one. **Reproduced against real broken states, not stubs**: a real absent volume (doctor reported it, `--fix` created it, the re-check confirmed it, a second run reported `rechecked: false`), and the real managed container stopped with `docker stop` (`--fix` started it, waited for readiness, and the re-check read `vector_db: reachable in 59.556 ms`, exit 0); both were restored and every probe volume and container removed. **Eight mutations, each proving a named test**: dropping the management-label, absent-container, and already-running guards; scoring an attempt without the re-check; passing the volume check when Docker is down; taking the exit code from the pre-fix report; repairing without `--fix`; and drifting the label written from the label read. **Salvaged from the abandoned worktree `agent-af26b20ddf611b53b` semantically, never copied** — it was written against a much older `main`. Its three-status vocabulary, the `FixOutcome` before/after shape, and the `as_dict` superset rule survived review; **rejected** were its `_check_vector_db_volume` returning a *passing* "not checked" check when Docker is down (T12), its per-attempt status taken from whether the fixer raised rather than from the re-check (the defect this task exists to avoid), its `volume_exists` docstring claiming a down daemon raises when `docker volume inspect` merely exits non-zero, and its unit fixture, which never stubbed `volume_exists` and so shelled out to a real daemon — passing on a developer machine that happened to have the volume and failing outright in CI, which has none)
- [ ] TASK-0258: Give `doctor --fix` a REST counterpart, or record the asymmetry as deliberate. `GET /v1/admin/doctor` stays read-only because a GET must not mutate, so the repairs are CLI-only — the T21 shape again (two surfaces, one shipped). Decide between a `POST /v1/admin/doctor/fix` and a documented "repairs are an operator action, not an API one"; related to TASK-0251's REST provisioning parity
- [x] TASK-0199: (security) `security.max_request_mb` was declared, documented, and enforced by nothing — ✅ 2026-08-06 (found by auditing all 108 schema settings against every consumer, the same declaration-versus-consumer check that found the dead CLI flags. `docs/security.md` §5 states 'Input size limits on all endpoints: `security.max_request_mb` (bodies)... oversize → 413 PAYLOAD_TOO_LARGE'. **Reproduced: with the limit set to 1 MB, an 8 MB body answered 200** — request bodies were entirely unbounded, which is a memory-exhaustion vector and a security control that read as configured while doing nothing. Implemented as ASGI middleware added last so Starlette wraps it outermost and it runs *before* authentication — checking after auth would still let an unauthenticated caller make the server read the whole body. Two checks, because either alone is bypassable: `Content-Length` refused before the body is read at all, and a running byte count for chunked uploads, which omit the header entirely. The count raises rather than truncating, since a handler handed a short read would answer as though the request were complete. 10 tests, including the chunked path a header-only check would miss. Three further settings are read by nothing and are now listed as deliberate, each against a task: `reliability.degradation_ladder` (TASK-0165), `cost.estimator` (TASK-0200), `autopilot.golden_set_size` (TASK-0201). A guard test fails on any new setting nothing reads, and on a pending entry whose task todo.md does not list)
- [x] TASK-0200: `cost.estimator` is declared and read by nothing — ✅ 2026-08-09 (gated rather than dropped, because config-reference.md sells the setting as the switch for D9. **Reproduced on `b76f811` before any fix**: with `cost.estimator: false`, `fasterrag estimate` exited `0` and priced the corpus, `POST /v1/estimate` answered `200` with a full estimate, `ingest --dry-run` printed 'would index 1 chunks from 1 documents', and `FasterRag.estimate` returned 9 tokens — four surfaces, none of them reading the switch. `require_estimator(settings)` lives in `services/estimation.py` and is called by all four. It refuses with `VALIDATION_FAILED` naming `cost.estimator`, never an estimate of zero: a report of no tokens and no cost is indistinguishable from a corpus that is free to ingest. CLI surfaces exit `2`, the endpoint answers `422` problem+json. **One deliberate exemption, pinned by its own test**: `benchmark --suite ingest` calls `estimate_sources` to *time* parse-and-chunk and reports no cost — verified by reading `_ingest_suite`, which uses the result only for durations and a `documents/tokens` dataset label — so the gate sits at the entry points rather than inside the function. 10 tests across the service, both CLI commands, the endpoint, and the facade; each was mutation-tested by reverting its gate and watching it fail. Removed from the `PENDING` dict in `test_settings_are_wired.py`; it was never in `loader.py::_reject_unenforced_settings` and must not be, since it defaults to `true` and that list refuses startup on a non-default value)
- [x] TASK-0201: `autopilot.golden_set_size` is declared and read by nothing — ✅ 2026-08-09 (**reproduced on `b76f811`**: with `golden_set_size: 40` in config, the parser supplied `args.size=100` and the generator was asked for 100 records; the setting appeared nowhere outside `schema.py`. The flag's own default is now gone — `--size` parses to `None` and `run_generate_golden_set` falls back to `settings.autopilot.golden_set_size`, so a configured `40` generates forty. An explicit `--size` still wins: the setting is the default, not a ceiling. The resolved value is also emitted in the `--json` document, since a number nothing prints is the same invisibility that let the mismatch hide — both defaults were 100, so the setting being ignored looked like nothing at all until somebody changed it. 4 tests parse through the real parser, because the defect lived in the default the parser supplied; each was mutation-tested by restoring the hardcoded default and watching it fail. Removed from the `PENDING` dict in `test_settings_are_wired.py`, which now holds only `reliability.degradation_ladder`; cli-reference.md no longer states a literal default)
- [x] TASK-0202: Audit API schema fields for ones no handler reads — ✅ 2026-08-06 (third application of the declaration-versus-consumer check, after the CLI flags and the config settings. Of 28 request fields, two were read by nothing. `IngestRequest.priority_class` is the **API twin of the CLI's `--priority-class`** — the same defect on the other surface, documented in api-reference.md as 'used by tiered embedding routing (D9)' and dropped by the handler, so a REST caller's routing class silently did nothing while the CLI's now worked. Both surfaces now merge it into chunk metadata identically. `ReplayRequest.diff_only` was declared, undocumented, and ignored; implemented as it reads — the two answer texts are the bulk of the response and the part a config sweep never opens, so it drops them and keeps the citations, which are how a caller finds which chunks moved. A guard test now fails on any request field no handler reads. **The error-code audit in the same pass found nothing**: all 27 codes are reachable, three of them only through their exception class's `default_code`, which a narrower check would have reported as dead)
- [x] TASK-0203: Gate the two reference documents against the surface they describe — ✅ 2026-08-06 (`fasterrag backup` and `restore` shipped absent from cli-reference.md while the parser's own docstring claimed the surface mirrors it exactly, found by accident under TASK-0138. **Both references audited and both are currently accurate** — 108 settings against 108 rows, 27 subcommands against 21 headings, no drift in either direction — so this landed as a guard rather than a fix. Nothing had been keeping them accurate: every row and heading added this session was hand-written, which is how the last gap appeared. Drift is checked both ways, because each direction fails differently: a setting with no row is undiscoverable, while a row with no setting sends an operator to configure a key the loader will reject. Verified by introducing both — an undocumented setting and a documented command that does not exist each fail their own test. **The first version of this check reported three false positives from its own regex**: `[a-z_]+` excluded digits, so every `bm25_*` row was invisible to it. Two tests now assert the parsers found something at all, since a pattern matching nothing reports perfect parity against an empty set)
- [x] TASK-0204: Verify the OpenAI generator against the real API, not a fake — ✅ 2026-08-06 (every unit test for this adapter runs against a faked client, which proves the mapping and says nothing about whether the vendor still speaks that shape — the caveat carried by TASK-0193, the OTLP work, and the archive work alike. Executed against the live API with `gpt-4o-mini` and a 32-token ceiling: **`complete()`** returned the text, reported 25 prompt and 1 completion token, and `finish_reason='stop'`; **`stream()`** delivered 6 incremental events with first token at 1.2s against 4.2s for the whole non-streamed call, and yielded no empty token — confirming the usage-only final event really is skipped rather than emitted blank; **truncation** at `max_tokens=5` came back as `finish_reason='length'` and `truncated=True`; **a rejected credential** classified as `GENERATION_FAILED` with `retryable=False`, which is what the circuit breaker needs — a permanent failure counted as transient would half-open on a bad key forever — and the key did not appear in the error detail. **The fakes were faithful**: no assertion behaved differently against the real vendor. Not added to the suite — a unit run must not depend on a credential or spend money; the equivalent live check belongs with the eval marker once there is a budget for it (TASK-0205))
- [ ] TASK-0205: Add a credential-gated live check for each configured LLM provider under the `eval` marker, skipping cleanly when the key is absent. The OpenAI generator was verified by hand against the real API under TASK-0204 and every assertion held, but nothing re-runs it, so a vendor changing its response shape would surface as a production failure rather than a red build
- [x] TASK-0206: Report a truncated answer and dropped evidence — ✅ 2026-08-06 (found while validating `openai_compatible` against a real third-party endpoint. Gemini's OpenAI-compatible API, given a 32-token ceiling, spent the whole budget on internal reasoning and returned **an empty answer with `finish_reason='length'`** — which fasterRag served as a plain `200` with `answer: ""` and no signal whatever. `Completion.truncated` existed, was docstringed, was unit-tested in isolation, and **was read by nothing**; so was `AssembledContext.truncated`. Both are now surfaced: the response carries `truncated` (the model stopped at `llm.max_tokens`) and `evidence_dropped` (chunks retrieved and reranked that did not fit the context budget — nothing cut off, but the answer is grounded in a subset of what retrieval chose). A truncated completion logs a warning naming whether the answer came back empty; dropped evidence logs the counts, which is what tells an operator to raise `chunking.chunk_size` or lower `retrieval.top_k`. The same probe confirmed the wire contract holds for a *second* vendor: base_url honoured, usage reported, streaming functional — though Gemini returns the whole answer as one event, so streaming buys no time-to-first-token there, unlike OpenAI's 1.2s against 4.2s. A guard now catches a computed property nothing reads, the variant the flag, setting, and schema-field guards all miss. **It matches by attribute name, not by type**, so two classes sharing a name mask each other — stated in the test rather than left as a silent weakness)
- [x] TASK-0207: Verify the OpenAI embedding adapter and the embedding cache against the real API — ✅ 2026-08-06 (two things were unproven. First, `config-reference.md` claimed hosted Matryoshka truncation works server-side; that sentence was written from the vendor's documentation under TASK-0186, never observed. **Confirmed**: `text-embedding-3-small` returns 1536 natively and exactly 256 when `embeddings.dimensions` asks, with the adapter reporting the same. Second, the embedding cache is a *cost* control, and unit tests can only prove it returns the stored vector — not that the provider was never reached, which is the thing being paid for. **Confirmed**: a repeat of two passages sent zero further texts and took 0 ms against 5.7 s cold, and a partial hit sent only the one new passage while placing the cached vector back in its original position — a reassembly bug there would silently pair texts with another text's vector, which no unit test using a fake provider would distinguish from correct behaviour. No code changed; both behaved as documented. The docs now say *verified* where they previously said *supported*)
- [x] TASK-0208: Apply the token-budget pass to every chunking strategy, not just `recursive` — ✅ 2026-08-06 (**TASK-0114 shipped incomplete and the tests did not notice**. The budget pass lived on `RecursiveChunker` alone, so `chunking.strategy: fixed` and `chunking.strategy: layout` still emitted chunks **3.6x their configured budget** on dense text — 166 tokens against a 46 limit — which is the exact defect TASK-0114 was written to fix. `late` was fine only because it wraps `recursive`. The pass now lives in `models.within_budget`, shared by all four, and the property tests fail for all four when it is disabled. Found by extending the chunking invariants to dense text: the existing properties generate ASCII only (codepoints 32–126), so the one input class the budget pass exists for was never generated. **The first version of that extension was vacuous and I nearly shipped it** — its alphabet included whitespace, so separator splitting broke every run into small pieces and the budget pass never triggered; disabling the pass failed none of the new tests. The strategy now excludes whitespace and sets a minimum length above the character limit, which is stated in a `# CRITICAL:` on the strategy itself. The lesson is the general one: a property test is worth exactly the inputs it generates, and the only way to know is to break the code and watch it fail)
- [x] TASK-0209: Property-test the identity invariants and close the digest's field-boundary collision — ✅ 2026-08-06 (document and chunk ids drive deduplication (D3) and lockfile drift (D1), where a collision is silent data loss — two distinct documents become one and the second is dropped as a duplicate. The 14 example tests covered the properties with one hand-picked collision pair; generated inputs found a **real one**: `_digest` joins its parts with a NUL, which separates only while no part contains a NUL, so `document_id('a' + NUL + 'b', 'c')` and `document_id('a', 'b' + NUL + 'c')` reached the same id. **Not exploitable today** — the tenant pattern rejects NUL and the other three `_digest` call sites pass hex digests, digits, or serialized config — but an invariant enforced only by a validator two modules away is one refactor from being false. `normalise_source` now refuses a NUL outright, which no filesystem or URL scheme permits anyway. Chosen over a length-prefixed encoding because that changes **every id ever minted** and so belongs at a release boundary with `IDENTITY_VERSION` bumped; the reasoning is recorded on `_digest` (TASK-0210). 12 property tests over unrestricted text, and the separator is written `chr(0)` rather than as an escape so the byte cannot land in the source file literally — which happened twice while writing this, once in the module and once in the test)
- [ ] TASK-0210: (maintainer decision, release boundary) Replace the NUL-joined digest in `core/identity.py` with a length-prefixed encoding, removing the field-boundary constraint rather than policing it at the one call site that takes user input. It changes every document and chunk id, so it needs `IDENTITY_VERSION` bumped to 3 and a reindex; harmless now, impossible after the first tagged release
- [x] TASK-0211: (security) Two tenants could address one backend collection — ✅ 2026-08-06 (collection isolation is carried by the *name*, `<tenant>__<name>`, because a vector database has no notion of our tenants. The validator forbade the separator *inside* a tenant id but allowed one to end with its first character, which reconstructs it across the boundary with a caller-supplied collection name: **`scoped_name('_b', 'a')` and `scoped_name('b', 'a_')` both produced `a___b`**, `visible_to` returned true for both tenants, and each saw it under its own name. Read *and* write, since the name is what the adapter upserts into — strictly worse than the replay disclosure (TASK-0194), which was read-only. A tenant id may now not end with `_`. With both rules the encoding is injective: a longer tenant sharing a prefix with a shorter one would need either the separator inside it or its first character at the end. **Two of my own test-design errors were caught while proving it.** The first strategy filtered tenants by restating the new rule, so reverting the fix left the generator unchanged and the injectivity property passed over inputs that could no longer collide; it now filters by *calling the validator*, so weakening the rule widens what is generated. The second: even widened, uniform search never finds a needle needing four correlated values, so the adversarial pair is constructed deliberately. Both failure modes are stated in the file, and reverting the fix now fails two tests rather than one)
- [x] TASK-0212: Property-test semantic cache isolation — ✅ 2026-08-06 (the third tenant-scoped surface, after the replay disclosure (TASK-0194) and the collection-name collision (TASK-0211). **No bug found** — the cache is correctly isolated, and is now proven so over generated vectors rather than asserted by a comment. The property that matters is the strongest form: an *identical* vector, similarity 1.0, must still not cross the boundary. It holds, including in a crowded cache where the scan walks other tenants' entries first, and `None` behaves as an owner rather than a wildcard. Removing the ownership check fails four of the eight tests. The design this confirms is worth recording: the check sits on the decoded *entry*, not the key, because lookup compares vectors across every stored entry and never reads keys — a key prefix alone would not isolate anything)
- [x] TASK-0213: Property-test context assembly — ✅ 2026-08-06 (assembly decides what evidence reaches the model, so a failure grounds an answer in something other than what the retriever chose. Eight of nine properties held: the budget binds, one citation per packed chunk, relevance order survives, duplicates are packed once, `empty` and `truncated` mean what they say. **The accounting did not reconcile.** A chunk carrying no text was skipped without being counted, so `used + dropped_duplicate + dropped_budget` came to less than the input — three chunks in, one accounted for — and a retrieved chunk could vanish with nothing reporting it. Now counted as `dropped_empty`. Our own chunkers guarantee non-empty chunks, so a non-zero value means the collection holds something they did not produce: an import, or a hand-written upsert. Low severity, but it is the kind of gap that makes a retrieval investigation start by doubting the numbers. The generator includes blank and whitespace-only text deliberately — a strategy that produced only real text could not have found it, which is the same mistake that made the first dense-chunking strategy vacuous (TASK-0208). The `

` separator in the assembled text is *not* re-split anywhere — `context.text` is only ever returned as the extractive answer — so the ambiguity a chunk containing the separator would create is not reachable, and was left alone rather than 'fixed')
- [x] TASK-0214: Run the D11 archive round trip against a live Qdrant — ✅ 2026-08-09 (the acceptance check TASK-0079 was left open for, unrunnable while no daemon existed. Six integration tests: a 12-point collection exports and re-imports, every vector and payload survives, a corrupted archive is refused before anything is written, and a dimension mismatch refuses the vector copy. **The whole path works.** Two things the fake adapter could never have shown. **Qdrant normalises on write** for a cosine collection, so `[1.0, .., 0.5, ..]` comes back `[0.894, .., 0.447, ..]` — the round trip preserves what the *backend* holds, not what was handed to it, and a test comparing against raw input would fail for the wrong reason. **Archives are not byte-identical.** The source comment claimed two exports of an unchanged collection produce identical bytes; they do not, because `manifest.json` carries a wall-clock `created_at`. Established precisely: every data member — `chunks.jsonl`, `documents.jsonl`, `vectors.jsonl`, `index.lock`, `checksums.sha256` — is byte-identical, and the manifest differs in that one field. The comment is corrected and the test asserts the true property rather than the claimed one)
- [x] TASK-0215: Recover the development environment after the host OS reinstall — ✅ 2026-08-09 (the venv pointed at a Python that no longer existed and held `cp313` wheels; the machine now runs 3.14.7 under a different user. Rebuilt on 3.14 — every pinned dependency resolves, including torch 2.13 and sentence-transformers 5.7 — and the full unit suite passes, so **3.14 is now known-good** where only 3.13 had been exercised. One real portability defect surfaced: `test_pooling.py` imported `torch` at module scope, so a fresh core-only install could not *collect* the unit suite at all, not merely skip those tests. It now uses `importorskip`, which is what the module docstring already claimed)
- [x] TASK-0227: Stop Hypothesis's deadline making the property suite timing-dependent — ✅ 2026-08-09 (with the Docker fleet running, the full unit suite failed a *different* property test on each run — semantic-cache tenant isolation once, tenancy name scoping the next — each passing in isolation moments later. Cause: Hypothesis fails any example exceeding 200 ms by default, and every property in this repo asserts behaviour rather than latency. A correctness suite that goes red for reasons unrelated to correctness is worse than a slow one: it teaches whoever sees the failure to re-run rather than investigate, which is precisely the habit that would let a real isolation failure through — and both flaky tests here happen to be tenant-isolation properties. A `tests/conftest.py` profile disables the deadline; performance belongs in the benchmark ledger on isolated hardware, not as a side effect of a property test. Verified with three consecutive clean runs of the affected packages)
- [x] TASK-0228: Property-test RRF fusion and BM25 encoding, and fix what the general forms exposed — ✅ 2026-08-09 (the example suite pinned each fusion invariant with one hand-picked leg pair, and two of the general forms were false. A repeated id inside one leg was scored once per occurrence, so `rrf_fuse(["a", "b", "b"])` returned `["b", "a"]` — a single leg reordering its own input — and `rank_in` reported the document's *worst* position because the later write won. Separately, a leg weighted `0.0` still contributed every document it ranked at score `0.0`; `QueryService` truncates the fused list to `candidates` and hands the whole shortlist to the reranker, which can promote any of them, so a leg an operator had weighted out of the fusion could still put chunks in front of the model. Both verified on `main` before the fix and re-verified by mutation afterwards. Two claims made alongside them did **not** survive measurement and were rewritten rather than shipped: `math.fsum` was documented as repairing a leg-order bug, but the pre-fix code's running total cannot reorder anything `rrf_fuse` accepts — a single fusion's contributions span at most ~5e4 while reassociation needs ~4.5e15, confirmed by an exhaustive sweep of the permitted k/weight/position values (1.2M combinations) and a 2M-trial random search over a wider space. `fsum` is kept, because the margin belongs to the current config ranges rather than to the algorithm, but it is now documented as a guarantee held rather than a bug found. The zero-weight properties also reached their case by filtering with `assume`, which discarded 59% of examples (measured) and left them vacuous on the rest; they now construct a silenced leg instead. Also corrects five dangling `TASK-0217` references to an id that was never in this ledger)
- [x] TASK-0229: Property-test the parsing layer and fix the eight defects the general forms exposed — ✅ 2026-08-09 (the example suite checked offsets, pages and sections on documents we wrote; this checks them on documents we did not, and every parser broke. **Text reaching the index:** a UTF-8 BOM survived `decode` because U+FEFF is not whitespace and `str.strip` keeps it — a BOM'd Markdown file lost every heading, a BOM'd CSV named its first column `﻿name`, and BOM'd JSON was rejected at line 1 column 1; the lenient fallback is a second `decode` call and needed the same fix. A whitespace-only CSV cell rendered as `column:   `, so a file of spaces and commas reached the index as a block of punctuation. A JSON `null` rendered as `": None"` and `""` as a bare `":"`. A `Heading 0` style gave a docx block outline depth 0, and since the builder keeps only entries strictly shallower than the incoming level, nothing is shallower than 0 — the entry never left the stack and every real h1 nested under it. **Errors escaping the taxonomy** (each a generic 500 with no problem+json body): `csv.Sniffer` can return a dialect whose delimiter equals its quote character, which `csv.reader` rejects with a plain `ValueError`, not even a `csv.Error`; `io.StringIO` translates newlines by default, so a lone CR raised `new-line character seen in unquoted field` on old-Mac files every spreadsheet opens; and any remaining `csv.Error` from the row loop is now a `ParseError`. **Resource:** `parse_document` read the whole file and then compared its length to `max_bytes`, admitting to memory the document the limit exists to keep out — the size now comes from `stat` and the file is never opened, verified by asserting `read_bytes` is not called. All eight reproduced on `main` first and all eight are mutation-verified. Two are unreachable by generation and are pinned as constructed cases: the `Heading 0` style, and a BOM in front of undecodable bytes. Both survived a mutation run before those tests were written, which is how they were found)
- [x] TASK-0230: Load the embedding tokenizer at pool startup instead of inside the first document — ✅ 2026-08-09 (found by a pre-commit run failing `test_a_full_queue_makes_the_parser_wait`, which then failed five times out of five in isolation and also failed on `main` — so not flaky, and not caused by the parsing work in the same commit. `create_token_counter` returns immediately but the tokenizer behind it loads on first use, measured at 5.4 s, and that first use was inside the first document's chunking: the pool starts, accepts work, and produces nothing for five seconds. It reads as a hang, it lands on whichever document happens to arrive first, and because the cache is process-wide it made the queue-backpressure test pass or fail purely on whether an earlier test had already warmed it — the same order-dependent redness TASK-0227 dealt with on the Hypothesis side. `CpuWorkerPool.__aenter__` now warms the counter and logs the load when it exceeds 0.5 s, so the cost sits at startup where a slow start is expected. `test_the_pool_loads_the_tokenizer_before_it_accepts_work` guards it by name rather than leaving the backpressure test to fail confusingly on its behalf; both were mutation-verified against removing the warm-up)
- [x] TASK-0231: Derive the OTLP signal path from `otel_endpoint`, and verify both signals against a running collector — ✅ 2026-08-09 (closes the "not verified against a real collector" note left open by TASK-0132 and TASK-0189, and fixes what that verification was always going to find. **The defect: one setting fed two exporters and no value worked for both.** OTLP/HTTP puts each signal on its own path, and the SDK only appends that path for the `OTEL_EXPORTER_OTLP_ENDPOINT` environment form — an endpoint passed explicitly is used verbatim, confirmed by reading `_endpoint` off both exporters. So a bare `http://collector:4318` 404s for traces *and* metrics, `.../v1/traces` 400s every metric push, and `.../v1/metrics` 400s every trace. Each is a logged warning and nothing else, so the toggle reads as on while nothing arrives. `signal_endpoint()` now treats the setting as the collector's base URL, exactly as the OTel specification treats that environment variable, and replaces rather than appends a path that already names a signal so existing configurations keep working. Spans also gained the `fasterrag` instrumentation scope the metrics payload already carried; spans arriving with an empty scope are attributed to nothing. **Verified against `otel/opentelemetry-collector-contrib` 0.140.1**: both exporters returned success, the collector logged no warning, refusal, or rejection, and its debug output showed five spans on trace id `169a6d2c…` with `query` as root and the four stages parented to it, `InstrumentationScope fasterrag` on both signals, `fasterrag_requests_total` as a `Sum` with `IsMonotonic: true`, `fasterrag_queue_depth` as a `Gauge`, and `fasterrag_request_duration_seconds` bucket counts summing to its observation count of 2. All four fixes mutation-verified against the unit suite)
- [x] TASK-0232: Ship the pgvector adapter, and prove the vendor-neutral contract against a second backend — ✅ 2026-08-09 (`adapters/vectordb/pgvector.py`, registered in the factory, reaching PostgreSQL through a libpq DSN named by environment variable rather than `vector_db.host`/`port` — a usable connection string already carries the host, database, role, and SSL mode, and it carries the password, which is why it is never written into `config.yaml`. Aliases are catalog rows rather than `ALTER TABLE` renames: a rename takes an ACCESS EXCLUSIVE lock and leaves the old collection unaddressable, which breaks blue/green reindexing. Snapshots are logical copies in the same database and are documented as such rather than dressed up as backups. **Two defects fixed before it landed.** The salvaged version defaulted `dsn_env` to `PGVECTOR_DSN`; every populated `*_env` field is collected by `referenced_env_vars` and required at startup by rule 9, so that default would have demanded a PostgreSQL DSN from every existing Qdrant deployment — confirmed by construction before changing it. It now defaults to `null`, with new cross-field rule 11 asking for it only when the provider is pgvector, and the shipped `config.yaml` leaves it null for the same reason. Separately, `_operator_sql` bound `clause` to `Composed` on its first assignment and then assigned a `Composable`, which `mypy --strict` rejected. **Verified against a live PostgreSQL 17 + pgvector**: the 32 shared contract cases in `tests/contract/vectordb.py` now pass on both Qdrant (96 tests) and pgvector (43), which is what makes 'any vector database' evidence rather than intent. **The contract itself had a hole, found by mutation**: nothing in it asserted anything about `score`. A SQL adapter orders by the distance operator directly, so returning a raw cosine *distance* as the score leaves every result correctly ordered with its score sign inverted — breaking pgvector's score expression that way passed all 43 cases. `test_a_closer_point_scores_higher_than_a_further_one` closes it for every adapter, and now fails under that mutation)
- [x] TASK-0235: Salvage the abandoned `cost.estimator` / `autopilot.golden_set_size` worktree into `main` — ✅ 2026-08-09 (20 uncommitted files in `agent-a5f3c20045cf98c7b`, based on `a0c5e16` while `main` had reached `b76f811`. Merged semantically per file rather than copied: a wholesale copy would have reverted TASK-0232's pgvector adapter and the `TokenPrice` rework of `services/estimation.py`, which had replaced the flat `PRICES_USD_PER_MILLION_TOKENS` table with per-entry `source`/`checked` provenance — the salvaged branch still carried the old shape. **No price data was taken from the abandoned work**, which is why nothing here needed a dated list price: its only change to `estimation.py` was the `require_estimator` guard, so the reconciliation was to keep `main`'s tables untouched. Both defects were reproduced on `b76f811` before either fix was accepted, and all nine gates were mutation-tested by reverting each and watching a named test fail. Landed as TASK-0200 and TASK-0201. **Rejected from the salvage**: the branch's `todo.md` prose claimed 11 and 4 tests and a `traces.replay` precedent for the exit codes — the counts were restated from what actually landed and the unverified precedent claim dropped. `loader.py::_reject_unenforced_settings` was deliberately left alone: `cost.estimator` was never in that list and cannot be, since it defaults to `true` and the list refuses startup on a non-default value; the equivalent exemption was the `PENDING` dict in `test_settings_are_wired.py`, and both settings are now removed from it)
- [x] TASK-0236: Verify the Langfuse trace export against a real server, and fix what that exposed — ✅ 2026-08-09 (closes blocker B7.1, which recorded that the export was verified only against a local HTTP stub, so field *names* were checked against Langfuse's documented API rather than against Langfuse. **Run against a real self-hosted Langfuse 3.225.1** (web, worker, Postgres, ClickHouse, Redis, MinIO): `LangfuseExporter.export()` returned True, the raw POST came back 207 with `successes` of length 5 and `errors` empty, and `GET /api/public/traces/{id}` returned the trace stored under fasterRag's own id — name `query`, the query text as `input`, and metadata carrying collection, mode, degraded, faithfulness, and retrieved count. All four stage observations came back with the right names and timings (retrieval 0.12s, reranker 0.29s, context-assembly 0.005s, generation 1.475s), `generation` typed GENERATION rather than SPAN, and its usage parsed as 386 prompt + 51 completion = 437 tokens. So the field names are right. **But the verification found a real defect on the way**: `export()` treated any 207 as success, and 207 is Langfuse's answer to *every* mixed outcome — the rejected events are listed in the body. Confirmed against the live server by sending a two-event batch with one invalid `type`: 207, one success, one error, and the exporter reported the trace exported. That is exactly the shape a wrong field name produces, so the old check could not have detected the very thing B7.1 was raised about. `export()` now reads the body and returns False when any event was discarded, logging the count and reason without failing the query. Mutation-verified: reverting it to a status-only check fails `test_a_partially_rejected_batch_is_not_reported_as_exported`)
- [x] TASK-0233: Salvage the abandoned real-fault chaos worktree onto current `main` — ✅ 2026-08-09 (an agent worktree based on an older `main` held `tests/chaos/test_real_faults.py`, `tests/chaos/disk_quota_probe.py`, and edits to three docs. Merged as semantic additions rather than copied, because copying would have reverted TASK-0226 and TASK-0190. **Three of its claims were stale or unmeasurable and did not ship.** (1) It asserted a stopped container reports `EMBED_PROVIDER_ERROR` and filed that as a defect; run against current `main` the assertion went **red** — the code is `RETRIEVAL_FAILED`, because TASK-0226 ✅ had already fixed it. The claim was dropped and the assertion corrected. (2) It filed `docker build` failing on the `.env.example` force-include; the `Dockerfile` already copies it, fixed under TASK-0190 ✅. Dropped. (3) It recorded the stopped-container failure as **78 ms**, which does not reproduce: four consecutive runs measured **44.2 ms, 5040.9 ms, 36.1 ms, 5043.9 ms** — bimodal, because Docker Desktop's port proxy sometimes accepts and hangs instead of the kernel refusing, in which case the bound is our own `reliability.timeouts.vector_db_ms` (5 s), not the OS's. The pinned figure was replaced with the assertion that actually holds, `elapsed < budget`, and the doc now records the range and the reason. It had also invented task ids — its `TASK-0216`/`TASK-0217`/`TASK-0218` are all taken on `main` by unrelated work. **Four mutations run, every one confirmed red before the suite was believed**: the stale error-code assertion failed on real code; disabling the probe's fill made the two ENOSPC cases fail; translating the journal's `OSError` made the defect-pinning case fail while leaving the loud-failure case green, which is the split those two are designed to have; and deleting the `.previous` rename from `_write_atomically` made the checkpoint case fail. That last one also **answered a question the salvaged probe never asked**: it defined `LATER_INDEX` and never used it, so nothing exercised a rewrite of an existing record. Measured, a *new* job cannot be written at `free_bytes: 0` while a *running* one can still checkpoint — `_write_atomically` renames the live record over the stale `.previous` copy first, freeing a file the size of the one it is about to write, and the mutation proves that is the mechanism. "Halt cleanly at a checkpoint" is therefore reachable on the very disk that caused the halt, which row 33 asserted and nothing had shown. Ten cases, `chaos` + `integration`, skipping cleanly without a daemon; the throwaway Qdrant uses its own name, ports 6733/6734, and volume so it can never touch the provisioner's)
- [x] TASK-0234: Translate a full filesystem into the typed error FMEA row 33 promises — ✅ 2026-08-10 (`Journal._write_atomically` let a raw `OSError` escape, so the row's "OS write error → typed `IngestionError`" was false and CLAUDE.md's mandatory taxonomy had a hole on the one path crash-resume depends on. Reproduced on `main` before touching it. The whole write — `mkdir`, the `.prev` rename, the write, and the publishing rename — is now wrapped, and a failure raises `IngestionError` chaining the original so **errno 28 survives translation**: a typed error that had forgotten it was `ENOSPC` would satisfy the taxonomy while telling an operator less than the raw error did, which is why the probe now records the chained cause and the test asserts both halves. Reported rather than swallowed-and-warned like a lost trace or lockfile, because losing a journal write quietly loses the ability to resume with nothing saying so. **The `.prev`-rename-before-write ordering was left exactly as it was** — TASK-0233 measured that it is what lets a running job checkpoint on the very disk that halted it, by freeing a file the size of the one about to be written. Verified against a real `ENOSPC` on a tmpfs, not only in simulation: the paired chaos assertion that TASK-0233 deliberately wrote to *expect the broken behaviour* was inverted in this same commit, per the T13 rule in rebuild-playbook.md, and reverting the translation turns it red again (9 passed, 1 failed). Two unit tests added; the first version of the temp-file-cleanup test was vacuous — patching `write_text` means no `.tmp` is ever created, so it passed with the cleanup deleted — and now injects the failure into the publishing rename instead. failure-modes.md row 33, rebuild-playbook.md T13, and release-readiness.md's gating row all moved with it)

- [x] TASK-0248: Make the suite test the checkout it is run from — ✅ 2026-08-09 (surfaced by a parallel agent whose first commit attempt failed for reasons that made no sense, and confirmed empirically: run `python -c "import fasterrag"` from any git worktree and it resolves to `D:\Github\FasterRag\src`, the *main* checkout. The editable install writes an absolute path into `site-packages/_editable_impl_fasterrag.pth`, so the working directory is irrelevant. Every agent worktree in this session therefore ran its own `tests/` against main's `src/` — and `pytest (unit)` is a pre-commit hook, so the gate agreed. The confusing direction is a new test for an unlanded fix failing; **the dangerous direction is a change whose behaviour main already had being committed green without its own code ever executing**, which is indistinguishable from a passing suite. Fixed by putting `src` first in `[tool.pytest.ini_options] pythonpath`, which pytest inserts at the front of `sys.path` ahead of the `.pth` entry. `tests/test_suite_targets_this_checkout.py` asserts the property directly rather than trusting it — it sits at the root of `tests/` because it is a property of the test *run*, not of any module. Verified by adding a throwaway worktree carrying the fix (passes), then removing `src` from the list again in that same worktree (fails, naming both paths). No behaviour changed, so no result already recorded in this ledger is invalidated — but every one of them was verified against main's source, which is the reason this is written down rather than quietly fixed)
- [x] TASK-0250: Stop two ledger entries from sharing one TASK- id — ✅ 2026-08-09 (found while checking whether this session's parallel agents had collided on ids: they had not, but three older pairs had. **TASK-0184, TASK-0185, and TASK-0186 each define two entries — six distinct pieces of work under three ids.** An id is how code, docs, and blockers.md point at a piece of work, so a shared one is a reference that resolves to two different things; `check_blockers.py` already relies on ids resolving uniquely. The cause is structural rather than careless: parallel work picks 'the next free id' by reading a ledger a sibling is about to extend, and today's fan-out reproduced the conditions exactly — one agent took TASK-0234 while another was still working under the same assignment. Live damage is limited: TASK-0185 and TASK-0186 are cited nowhere outside the ledger, and TASK-0184's two citations both mean its dashboard entry. `check_doc_truth.py` now counts entry-defining lines and fails on any id defining more than one, with the three existing pairs recorded in `KNOWN_DUPLICATE_IDS` so the gate reports growth rather than history — ticked entries are append-only and frozen, so renumbering them is not a gate's call. Three tests cover the collision, the clean ledger, and the case that would have made it useless: an id merely *mentioned* in another entry's prose, which happens constantly, must not count as a definition. Mutation-verified. The gate's failure text was also split by violation class — it previously answered a duplicate id with 'update the document or delete the code', and a gate that prints the wrong remedy teaches people to ignore the right one)
- [x] TASK-0249: Cover the untested CLI wrappers, and fix the seven defects that coverage exposed — ✅ 2026-08-09 (salvaged from the abandoned worktree `agent-a50d98db58d084a36`, based on `a0c5e16` while `main` had reached `8dff033`. Merged semantically per file rather than copied: a wholesale copy would have reverted TASK-0200's `require_estimator` guard, TASK-0201's `golden_set_size` fix and its test file, and TASK-0248's `pythonpath` change. `tests/unit/cli/` had no suite for `dispatch`, `serve`/`worker`, `backup`/`restore`, `export`/`import`, `benchmark`, `replay`, `traces`, or the golden-set generator; **+145 tests** now cover them, and the package gained the `__init__.py` every other test package already had. **Seven defects, each reproduced against `8dff033` before its fix was accepted, and every fix mutation-verified by reverting it and watching a named test go red.** (1) `benchmark --suite ingest --sources ./corpus --ledger` took the directory verbatim, so it timed one failed read and printed a pasteable `BENCH-0001` entry reading "Dataset: 0 documents, 0 tokens" beside a throughput figure — a published performance number backed by nothing, which is the exact thing the provable-claims policy exists to stop. Sources are expanded, and an expansion that finds nothing is refused with exit 2 rather than measured. (2) `estimate ./corpus` priced the whole corpus as one unreadable document and quoted every provider at $0.0000 while exiting 0; a preflight that does not price what the ingestion after it would read is not a preflight. Expansion runs after the `cost.estimator` refusal, because a disabled command has no business walking the filesystem. (3) `autopilot generate-golden-set ./corpus` handed the raw directory to the generator, the same TASK-0188 shape a third time. (4) `index reembed` already read `args.recursive`, which the parser declared nowhere — the handler was written for a flag argparse rejected with exit 2; `--recursive` is now declared on `reembed` and on `estimate`, matching `ingest`. (5) `index reembed --watch` was accepted and silently ignored, so TASK-0196's claim that "both declare the flag and print a not-implemented notice today" was true of `ingest` only; `test_flags_are_wired.py` could not catch it because it searches every CLI module at once, so `ingest`'s notice satisfied the assertion on `reembed`'s behalf. (6) `export` and `import` returned exit 1 for an unreachable backend, where every other command returns 3 and line 16 of `cli-reference.md` documents 3 as universal — the two commands a retry wrapper could not branch on. (7) `worker` reported `cpu=0` straight from `workers.cpu_pool_size`, documented as "0 = auto", while the pool it was about to start resolves 0 to the CPU count: measured, an operator was told 0 parse workers where 16 would run. **Three things were rejected or rewritten rather than salvaged.** The branch's `test_dispatch.py` was broken: `from fasterrag.cli import main as cli_main` binds the re-exported *function*, not the module, so all seven monkeypatches of `_HANDLERS`, `PENDING_COMMANDS`, and `_dispatch` raised `AttributeError` and asserted nothing. Its `--recursive` assertions built an `argparse.Namespace` by hand, which passes whether or not the parser declares the flag — exactly the defect under test — and now go through `build_parser`. And its benchmark test asserted only the reported dataset label, which the mutation run caught: reverting the expansion inside the *timed* closure left it green, because the label comes from a second call. `test_the_timed_work_is_the_expanded_corpus_and_not_the_directory` spies on every `estimate_sources` call, and fails under that mutation)
- [x] TASK-0257: Build the vector-DB circuit breaker — ✅ 2026-08-10 (the last outbound call on the query hot path with no shedding, and the one with nowhere to degrade to: a reranker failure drops to `hybrid_only` and a generation failure drops to `extractive`, but a retrieval failure means there is nothing to answer *from*, so it surfaces. That is exactly why the breaker belongs there — without one a backend that is down is searched again on every request, and those retries are the load keeping it down. `RetrievalService` now owns a `vector_db` breaker for its lifetime and guards the legs with it; `CircuitOpenError` already maps to a retryable 503 through the problem table, so shedding reports itself as the temporary condition it is. **A misattribution was caught while writing the test and fixed before it shipped**: the query embedding is an *embedding provider* call that happens to sit on the retrieval path, and it was inside the guarded region — an embedding outage would have opened the vector-database breaker, shedding reads from a backend that was perfectly healthy and hiding the real provider behind the wrong gauge. The embed now happens outside the guard, pinned by a test that fails when it is moved back. Four properties mutation-verified: the check, the failure record, the success record, and the misattribution. With this, reliability.md's "one breaker per provider (LLM, embeddings, vector DB)" is true for the first time — it was true for one of three when TASK-0245 was opened)

- [x] TASK-0259: Finish the abandoned pgvector pooling worktree — ✅ 2026-08-11 (salvaged from a worker stopped mid-verification, which is where the interesting part was. Its rendezvous helper `wait_until_blocked` was being passed the `blocker` connection — the one *holding* the lock — and the fixture's own CRITICAL comment explains why that cannot work: PostgreSQL runs `stats_fetch_consistency = cache`, so the statistics a transaction reads are frozen for its whole life, and polling from inside the transaction that holds the lock returns the pre-block snapshot forever. **The test did not pass vacuously — it failed honestly**, raising "no backend ever waited on the lock, so nothing in this case actually overlapped" rather than proving nothing quietly, which is the behaviour every concurrency test in this repo should have. It had built the separate autocommit `observer` fixture to fix it and had wired two of three call sites. The third is now wired. Also completed what it never reached: both settings documented in config-reference.md (the reference-parity gate was red on exactly those two keys), the example block updated since it claims to show all defaults, `ClassVar` annotations, and the two mutation runs. 50 integration cases green against a live PostgreSQL, including the shared adapter contract suite)
- [ ] TASK-0260: Emit `estimated_cost_usd` in the query response body. Split out of TASK-0242, which delivered the *enforcement* half of D9 and left this one untouched: `data-model.md` specifies the field on `usage`, `differentiators.md` names it, and the string appears nowhere in `src/`. Per-query cost currently reaches operators only through the `fasterrag_cost_usd_total` metric, so a caller holding a response cannot see what it cost. The pricing side already exists — `services/estimation.py` carries dated list-price tables with per-entry provenance and `price_generation` is already called on the generation path for the metric — so this is a response-shape change, not a costing one. A model with no recorded rate must contribute nothing rather than a fabricated number, which is why the field has to be nullable rather than defaulting to zero
- [ ] TASK-0261: **CI on `main` has been red since 2026-08-04 and nothing noticed.** Established while investigating TASK-0252: of 100 recorded `ci.yml` runs on `main`, 19 are green and the last of those is `eb39bdda` (2026-08-04); every run since — roughly sixty commits, including all nineteen of 2026-08-09/12 — has failed. CLAUDE.md's trunk rule says every commit must leave `main` green, so the rule has been continuously breached for eight days. **Why nobody saw it**: the pre-commit hook runs the gates locally and passes, so every commit looked green to the person making it, and nothing reads the Actions API. The 2026-08-10 release-readiness review scored process integrity 95 and cited the CI gates as evidence — while those gates were failing. On the latest run (`6b30ef54`) four jobs fail: **quality/Typecheck**, **supply-chain/Lockfile is current**, **integration/Adapter contract suite**, and **windows/Unit tests**. Notably the coverage gate is *not* among them. The lockfile failure is fixed here — `uv.lock` was missing `psycopg` entirely and `uv lock --check` now passes — but the other three could not be reproduced: **the dev environment is Python 3.14.7 while CI pins 3.12**, two minor versions apart, so local green says little about CI; job logs need admin rights (403 unauthenticated) so the actual errors are unread; and Docker is down, so the integration job cannot be exercised. **Needed**: read the four job logs with an authenticated `gh`, fix each, and then make red visible without a human remembering to look — a required status check on `main`, or a `doctor`-style command that reads the Actions API. A gate whose failure nobody sees is not a gate
- [x] TASK-0262: Score Redis cache recency with a server-side sequence, not the client clock — ✅ 2026-08-12 (found by reproducing the `windows` CI job that TASK-0261 records as failing: `test_eviction_drops_the_least_recently_used` passes on this machine's Python 3.14 and fails on the 3.12 CI pins. **Measured rather than guessed**: `time.time()` resolves to 15.625 ms on Windows before CPython 3.13 — 2000 rapid calls returned **two** distinct values, against 1173 on 3.14 — so every entry touched inside one tick scored identically and Redis broke the sorted-set tie by comparing members lexicographically. **Eviction was alphabetical rather than least-recently-used**, and systematically: a hot key early in the alphabet was evicted again and again while a cold one late in it survived. That is a real defect on a supported platform, not a test artifact — `requires-python` is `>=3.12`. No epoch-based high-resolution clock exists there to switch to: `time`, `time_ns` and `monotonic` all returned a single distinct value in 2000 calls, and `perf_counter`, which is precise, is process-relative and so useless for a cache several replicas share. Recency is now an `INCR` on a per-namespace sequence key: strictly increasing, atomic, identical for every replica, and independent of any clock — which also removes a hazard the wall clock always had here and nothing had recorded, that Redis is the backend chosen *because* replicas share it and their clocks do not agree. Costs one round trip per touch. **The regression test was rewritten rather than reused**: the existing one cannot catch this on a modern interpreter, because a 100 ns clock makes the ties vanish, so a new case freezes `time.time()` outright and fails on every platform if recency ever returns to a wall clock — mutation-verified red on 3.12 *and* 3.14. Full suite green on both interpreters)
- [ ] TASK-0191: (maintainer review) `VectorDBAdapter` gained `delete_snapshot` under TASK-0138. Backup retention cannot reclaim space without it: a snapshot lives inside the backend, so pruning a set without deleting its snapshot leaves the snapshot orphaned and invisible to every manifest. Qdrant has `delete_snapshot`; a backend without one could not satisfy the contract. Same boundary question as TASK-0126, TASK-0133, and TASK-0182 — confirm or revert

### S12 — Autopilot v1

- [x] TASK-0086: Implement suggest-only eval-driven auto-tuning with measured deltas; assert zero writes to config.yaml (D6) — `fasterrag autopilot run` searches query-time parameters against a golden set, writes `autopilot-suggestion.yaml`, and verifies config.yaml is byte-identical afterwards; searching index-time parameters is TASK-0145 — ✅ 2026-08-02
- [x] TASK-0144: D6's acceptance test — "on a fixture corpus with a known-better config, autopilot's suggestion matches or beats it" — now verified against a real corpus, not only by unit test. On `tests/eval/datasets/policies` the known-better configuration is BM25-weighted retrieval (nDCG@5 0.9308, MRR 0.9062, measured independently) because the discriminators are literal tokens — a year, a region, a role — that BM25 matches exactly and dense embeddings blur. Started from the default 1.0/1.0 weights, autopilot evaluated 7 candidates in 33 s and suggested `bm25_weight=1.0, dense_weight=0.5` at **nDCG 0.9308 / MRR 0.9062 — matching the known-better score exactly**, reported as `ndcg +0.0231, mrr +0.0312`. `config.yaml` was confirmed byte-unmodified afterwards and the suggestion was written to a separate file marked NOT APPLIED. Recall stayed at 1.0000 across all seven trials, which is exactly why the handbook fixture could not run this test — ✅ 2026-08-03
- [ ] TASK-0145: Autopilot searches query-time parameters only. Chunk size, overlap, and the embedding model change how the index is *built*, so each candidate costs a full re-chunk and re-embed of the corpus — hours on a real one. That search needs its own budget model and a temporary collection per candidate

### S13 — Langfuse + Grafana integrations

- [x] TASK-0043: Implement Langfuse auto-provisioning (compose stack, secrets generated once and preserved, LANGFUSE_INIT_* headless bootstrap without double quotes, doctor-gated, return http://host:3000, zero code changes at toggle time) — verified end to end: the six-container stack came up, `/api/public/health` returned `{"status":"OK","version":"3.224.4"}`, and the bootstrapped keys authenticated against `/api/public/projects`, returning the `fasterRag` org and project with no quote characters in either name; doctor-gating is TASK-0149 — ✅ 2026-08-02
- [x] TASK-0044: Implement Grafana auto-provisioning (provisioning-as-code datasources/dashboards, editable:false, allowUiUpdates:false, 30 s reload) — verified end to end: fasterRag → Prometheus → Grafana's provisioned datasource returned real series — ✅ 2026-08-02
- [x] TASK-0154: The provisioned dashboard rendered six empty panels. Three causes, all fixed: five catalogue metrics (`queue_depth`, `dlq_depth`, `ingest_documents_total`, `ingest_throughput`, `cost_usd_total`) were declared but written by nothing; the Prometheus host port and container port had been collapsed into one constant, binding 9099:9099 where nothing listens; and no panel carried a `legendFormat`, so each legend rendered the whole label set and truncated before the only label that varied. Verified live: ingest and query over HTTP moved every wired series, and Grafana's datasource proxy returned them — ✅ 2026-08-02
- [x] TASK-0146: `observability.grafana: true` does not yet trigger provisioning at startup — `fasterrag provision grafana` does. The doc frames the toggle as the trigger, and the config-driven path should call the same provisioner so flipping the flag is genuinely all it takes — `provision_enabled_observability` runs in the API lifespan, so the toggle really is the trigger the doc describes. A provisioning failure is logged and swallowed: refusing to serve queries because a dashboard would not start inverts the dependency — ✅ 2026-08-03
- [x] TASK-0147: Grafana provisioning is not doctor-gated. The Qdrant provisioner checks `fasterrag doctor` before mutating anything; the Grafana one should use the same gate so a port conflict is reported with a fix rather than as a container that fails to start — `require_provisioning_gate` extracted from the Qdrant provisioner and called by Grafana before any manifest is written — ✅ 2026-08-03
- [x] TASK-0148: `fasterrag_circuit_state` is the one catalogue metric nothing writes, because no circuit breaker exists — only its configuration under `reliability.circuit_breaker`. The dashboard's circuit panel is empty by design until the breaker in `docs/reliability.md` §3 is implemented, and says so in its panel description; `test_no_catalogue_metric_is_declared_and_never_written` pins this as the only exemption — ✅ 2026-08-06 (`core/breaker.py`, three states per the spec, wired into the embedding pool's retry loop. `reliability.circuit_breaker.enabled` **defaults to true**, so until now the shipped default advertised a breaker that did not exist. Measured against a stub provider that is permanently down, ingesting 40 chunks: **80 provider attempts and 60 retries without it, 5 attempts and 3 retries with it** — a 16× reduction in calls to a provider already known dead, with all 40 chunks still dead-lettering correctly so nothing is silently lost. A demonstration of behaviour against a stub, not a benchmark-ledger entry. Only *retryable* failures count, because a bad API key fails identically forever and counting it would present a permanent misconfiguration as an intermittent outage. **A real bug was caught by its own test**: `record_failure` read the raw state field while the half-open transition is lazy on the property, so a failed probe on a breaker nothing had observed left `_opened_at` stale and would half-open again on the very next call — one probe admitted per call instead of per timeout. The checked-in flow calls `check()` first and would have hidden it. `KNOWN_UNWRITTEN` is now empty: every catalogue metric has a write site. Does not close TASK-0165, which also covers the `cache_only` rung (TASK-0159))
- [x] TASK-0149: Langfuse provisioning is not doctor-gated either, and needs the same gate as TASK-0147. Its eight published ports are the widest surface any toggle claims, and a conflict currently surfaces as a Compose failure after some containers have already started — gated before any secret is generated, since a conflict discovered mid-`compose up` leaves some of the eight containers running and some not — ✅ 2026-08-03
- [x] TASK-0150: `observability.langfuse: true` does not trigger provisioning at startup — `fasterrag provision langfuse` does. Same gap as TASK-0146 on the Grafana side, and both should be fixed by the same config-driven path — same startup path as TASK-0146, both toggles handled together — ✅ 2026-08-03
- [x] TASK-0151: Export traces to the provisioned Langfuse — `observability/langfuse_export.py` maps a `Trace` onto Langfuse's ingestion API and the trace store ships every trace it writes. The local copy is still authoritative: replay and `GET /v1/traces/{id}` read it, so an incident investigation never depends on the observability stack being healthy. Export is fire-and-forget with the task retained until it completes (asyncio holds only a weak reference, so an unretained task can vanish mid-flight), and a failure is logged and dropped because the query was answered long before it ran. The generation stage is sent as a `generation-create` rather than a `span-create`, because Langfuse derives model usage and cost only from that type. **Verified against a real local HTTP server** — path `/api/public/ingestion`, Basic auth header, 3-event batch, and 207/401/unreachable all handled — but **not yet against a live Langfuse**: Docker Desktop was down when this landed. That last mile is TASK-0178 — ✅ 2026-08-03
- [x] TASK-0178: Confirm the Langfuse export against a live stack — start the provisioned containers, run a query with `observability.langfuse: true` and the generated keys in `.env`, and check the trace appears in the UI with its four spans and the generation's token usage. The wire format is unit- and socket-tested (TASK-0151 ✅) but no real Langfuse has accepted a batch, so the field names are verified against the documented API rather than against the server — ✅ 2026-08-09 (**executed as TASK-0236**, which ran it against a real self-hosted Langfuse 3.225.1: the batch was accepted, the trace came back under fasterRag's own id with all four stage observations and the generation's 386+51 token usage, and the exercise found a real defect — a 207 with rejected events was being reported as a successful export. Ticked here by the 2026-08-09 claims audit, which found this entry still open and still asserting "no real Langfuse has accepted a batch" after that had stopped being true)
- [x] TASK-0152: `GENERATION_PRICES_USD_PER_MILLION_TOKENS` covers only `gpt-4o-mini` and `gpt-4o`. Every other generation model contributes nothing to `fasterrag_cost_usd_total`, so the cost panel silently understates spend rather than reporting an unknown. The table needs the remaining configured providers, and the panel needs a way to say "some traffic is unpriced". (The metric itself is now verified live: a real query recording 386 prompt and 51 completion tokens on `gpt-4o-mini` published `8.85e-05`, matching `386×0.15/1e6 + 51×0.60/1e6` exactly. Superseded in scope by TASK-0168.) — ✅ 2026-08-09 (**Priced**, each rate read from the vendor's own pricing page on 2026-08-09 and recorded with that source and date on the entry itself: *OpenAI generation* — 33 chat/reasoning models (`gpt-5.6-sol/-terra/-luna`, `gpt-5.5`/`-pro`, `gpt-5.4`/`-mini`/`-nano`/`-pro`, `gpt-5.2`/`-pro`, `gpt-5.1`, `gpt-5`/`-mini`/`-nano`/`-pro`, `gpt-4.1`/`-mini`/`-nano`, `gpt-4o`/`-2024-05-13`/`-mini`, `o1`/`-pro`, `o3`/`-pro`/`-mini`, `o4-mini`, `gpt-4-turbo-2024-04-09`, `gpt-4-0613`, `gpt-3.5-turbo`/`-0125`/`-1106`) from developers.openai.com/api/docs/pricing, which also re-confirmed the three OpenAI embedding rates already in the table; *Anthropic* — 14 ids (`claude-fable-5`, `claude-mythos-5`, `claude-opus-5`, `claude-opus-4-8/-4-7/-4-6/-4-5` + dated form, `claude-sonnet-5/-4-6/-4-5` + dated form, `claude-haiku-4-5` + dated form) from platform.claude.com/docs/en/about-claude/models/overview, with `claude-sonnet-5` recorded at its standard $3/$15 and a note that the introductory $2/$10 runs to 2026-08-31, since over-estimating is the safer error; *Cohere* — `command-r-plus-08-2024` alone, the one per-token rate cohere.com/pricing still quotes verbatim; *Ollama and HuggingFace* — zero for every model, now stated as a deliberate recorded price rather than an absence, so local traffic is neither a cost nor an unpriced gap. **Left unpriced on purpose**: Cohere's current Command A / R7B lineup, whose pricing page publishes instance-hour Model Vault rates — converting those to a per-token rate needs a throughput measurement nobody has taken, and an unmeasured number is a defect by policy; `openai_compatible` gateways, which set their own prices; OpenAI's completion-only models (`davinci-002`, `babbage-002`, `gpt-3.5-turbo-instruct`), unreachable through the adapter's chat-completions call; and Cohere's three embed rates, which the pricing page no longer states per token — those keep their 2026-07-30 date rather than being re-dated to a check that did not happen. **Restructured**, because the old shape could not hold this: entries are `TokenPrice` records carrying provider, source, and check date individually. One global `prices_dated` is only ever as true as its oldest row, so re-checking one vendor silently re-dated every stale row beside it; `PRICES_DATED` survives as the derived minimum and can no longer claim a freshness no entry has. Matching now requires **provider as well as model** — the old table billed an OpenAI-compatible gateway serving `gpt-4o` at OpenAI's rate, a fabricated figure it produced silently. Renames: `PRICES_USD_PER_MILLION_TOKENS` → `EMBEDDING_PRICES`, `GENERATION_PRICES_USD_PER_MILLION_TOKENS` → `GENERATION_PRICES`, `PRICES_SOURCE` removed. 16 tests in `test_cost_metric.py`, four of them driving a real query through `GenerationService` to pin the metric behaviour: a priced model adds exactly its list price to `fasterrag_cost_usd_total`, an unknown model and a known model on an unknown gateway each add their full token count to `fasterrag_unpriced_tokens_total` and nothing to the cost, and a local model adds to neither. Panel wording and per-model coverage documented in config-reference.md, observability.md, and integrations.md. The panel's "some traffic is unpriced" half was TASK-0168.)
- [x] TASK-0153: Filed on a mis-read: a query was reported as hanging past ten minutes. It does not. Measured on the same deployment, a single query returns `http=200` in **10.78 s**; the ten minutes were my own shell budget — an ingest, a 25 s sleep, and three curls each capped at `-m 120` — not one unbounded call. `reliability.timeouts.llm_ms` is applied by the adapter at construction (`adapters/llm/base.py`), and the openai adapter passes it to its client. No defect; entry kept rather than deleted so the ledger records the correction — ✅ 2026-08-02

### S14 — Observability dashboard (last; after Langfuse proves the trace pipeline)

- [x] TASK-0045: Implement read-only self-hosted dashboard — a **separate ASGI application on its own port**, never mounted into the control-plane API. Two structural reasons: it declares no write route at all, so "observability-only" is a property of the application rather than a promise about restraint; and it shows prompts, responses, and corpus text, so an operator must be able to bind it to an internal interface while the API faces callers. Reads the metrics registry through a new public `Registry.series` accessor so the page and a Prometheus scrape cannot disagree. Every interpolated value is escaped — a trace carries user text and model output straight onto an operator's page. **Verified live over a real socket**: `GET / -> 200` showing a stored query, its tenant, and a recorded metric; `POST / -> 405`. 11 tests, including the no-write-route assertion and an XSS attempt — ✅ 2026-08-04
- [x] TASK-0184: Tenant-scope the dashboard — it now mounts the **same `AuthMiddleware` the API uses** rather than a second implementation, so it inherits authentication, scopes, and tenant matching in one place; a separate auth path would be a second thing to get wrong, and one would eventually lag the other. With `security.auth` off it stays open, which is the single-operator deployment it was built for. With tenancy on, the middleware refuses any request that does not name a tenant, and both the page and its JSON twin filter traces to that tenant — scoping the page alone would have left the leak one URL away. Its routes require `admin`, the same scope `/v1/traces` and `/metrics` need, because it reads the same data. 16 tests — ✅ 2026-08-04

### Cross-slice (scheduled opportunistically after their dependencies)

- [ ] TASK-0049: Implement remaining vector DB adapters (Milvus, Weaviate, Pinecone, pgvector, Chroma) passing the contract suite — pgvector first: it proves the contract against a genuinely different (SQL) paradigm and is the cheapest to CI
- [x] TASK-0050: Ship Docker deployment artifacts per deployment.md (compose profiles, sizing presets) — ✅ 2026-08-06 (`Dockerfile` — two-stage, non-root uid 10001, healthchecking `/readyz` rather than `/healthz` because the latter answers before the vector database is reachable and a load balancer would send traffic too early. `docker-compose.yml` with the `core`, `qdrant`, and `dashboard` profiles, plus `deploy/compose.{small,medium,large}.yml` mapping to the sizing table. **Langfuse and Grafana are deliberately excluded**: both are already provisioned by their config toggle, and restating them would create a second source of truth that drifts the first time a pinned version changes on one side. Published ports bind to `127.0.0.1` — the API serves unauthenticated until `security.auth: true` and the dashboard renders prompts verbatim, so publishing on every interface would expose the corpus at startup. **A fabrication of mine was caught and fixed rather than shipped**: the presets referenced a `FASTERRAG_SET` environment variable that did not exist and would have silently done nothing, so it was implemented for real — the env form of `--set`, applied beneath explicit flags and validated identically. **Verified**: all four compose files pass `docker compose config`, each profile resolves to exactly the documented services, and the presets' limits and overrides appear in the resolved output. **The `Dockerfile` has never been built** — that needs a daemon, blocker B7)
- [x] TASK-0190: Build and run the `Dockerfile`, and bring the `core`/`qdrant` profiles up for real — ✅ 2026-08-09 (**the artifacts were broken. Five defects, none of which `docker compose config` can see, which is the only thing that had ever been run against them.** (1) **The image could not be built at all, ever.** The builder stage copies `pyproject.toml`, `README.md`, `src/`, and `config.yaml`, but the wheel target force-includes `.env.example` as well (TASK-0170 ✅), so hatchling aborted with `Forced include not found: /build/.env.example` during metadata generation — before one dependency resolved. `.dockerignore` already re-includes the file past its own `.env.*` rule, so only the `COPY` was missing. (2) **`/readyz` could never have passed.** `config.yaml` ships `vector_db.host: localhost`, right on a host and wrong in a container, and nothing overrode it: measured, the api answered `503 dependencies are not ready: vector_db` on every probe and the worker exited `VECTOR_DB_UNAVAILABLE` into a restart loop. Fixed with `FASTERRAG_SET: vector_db.host=qdrant` in the base file, repeated in all three sizing presets — compose merges `environment` per key, so a preset that sets `FASTERRAG_SET` replaces the base value outright instead of extending it, and the presets would otherwise have re-broken what the base file fixed. (3) **The worker reported itself unhealthy forever.** A container inherits its image's `HEALTHCHECK` whatever command it runs, and the image probes `:8000`, which a worker never binds; the compose comment claiming the worker has "no healthcheck" described an intention, not the file. Now `healthcheck: disable: true`. (4) **The `dashboard` profile started no dashboard.** It ran `serve --port 8080`, which moves the *API* onto 8080 — the dashboard only starts when `observability.dashboard` is on, so the profile published a second copy of the API under the dashboard's name, with an inherited healthcheck pointing at a port nothing bound. Now sets `observability.dashboard=true` and leaves the API on 8000, which makes the inherited healthcheck correct rather than needing an override. (5) **`deploy/compose.large.yml`'s `ports: []` drops nothing.** Compose merges sequences by appending, so the base file's `127.0.0.1:8000:8000` survives into the rendered config; with `replicas: 3` that is two api containers that cannot bind their host port. The comment justified the empty sequence as the standard-YAML alternative to `!reset` — it parses cleanly and does nothing, which is the worse failure. Replaced with `ports: !reset null` and that one file excluded from the `check-yaml` hook, since `yaml.safe_load` rejects the Compose-only tag. **Measured** — Docker 29.6.2 / Desktop 4.85.0, Windows 11 + WSL2, compose project `fasterrag-build-probe` on shifted host ports so it could not touch the running dev fleet: `docker build -t fasterrag:local .` takes **178 s** cold (`--no-cache`, base image already local) for a **501 MB** image (no extras, `python:3.12-slim`, Python 3.12.13 — the `ARG PYTHON_VERSION=3.12` pin builds and runs fine even though the host now develops on 3.14); `docker run --rm fasterrag:local --help` prints the CLI help and exits 0; `id` reports `uid=10001(fasterrag) gid=10001(fasterrag)` and `/app/.fasterrag` is writable both as a baked image directory and through the mounted named volume; `core` + `qdrant` + `dashboard` all reach `healthy`, `/readyz` returns `200 {"status":"ready"}` with `vector_db` "reachable in 8.862 ms" from inside the container and through the published port, the worker reaches `worker ready: pools=cpu,embed,index`, and the dashboard serves `200` on 8080; under `deploy/compose.small.yml` the limits land for real as `2 CPU / 2 GB` on the api and `4 GB` on the worker, with `FASTERRAG_SET` merged as `vector_db.host=qdrant,app.workers=2`. Torn down with `down -v`. **Two things stay unmeasured**: the stack was exercised on the no-extras image, because `docker compose up` reuses an existing `fasterrag:local` rather than rebuilding, so compose's default `EXTRAS: huggingface` only adds `sentence-transformers` on top of what was verified and `/readyz` does not touch it — and that build itself was attempted and **abandoned after 20 minutes** without finishing its dependency download, which is the same multi-gigabyte first-run cost TASK-0174 ✅ measured from the other side. An operator's very first `docker compose up` pays it)
- [x] TASK-0226: A Qdrant connectivity failure still reports `EMBED_PROVIDER_ERROR` — ✅ 2026-08-09 (every backend failure now translates to `RETRIEVAL_FAILED`, which is what `docs/api-reference.md` has always promised for a vector-DB outage — **the doc was right and the code was wrong**. The two render different problem documents, so a stopped Qdrant reported as 'Embedding provider failed' sent an operator to inspect a healthy embedding provider while their vector database was down, and made a real embedding outage indistinguishable from this one. Verified against a genuinely unreachable instance: `code=RETRIEVAL_FAILED retryable=True`. Auth failures keep their own non-retryable `VECTOR_DB_AUTH_FAILED`, since a rejected key fails identically forever and naming the outage would be the wrong answer in a confident tone. Salvaged from the TASK-0225 agent, which was terminated by a session limit mid-fix)
- [ ] TASK-0087: Publish the fasterrag package to PyPI (wheel, extras, hash-locked deps, SBOM) at the first tagged beta. Distribution mechanics are now verified: `python -m build` produces both artifacts, `twine check` passes on both, and the wheel installs into a clean venv and runs. **Remaining blockers before a first publish** — TASK-0164 (license decision; the only irreversible one, since a PyPI version number can never be reused) and TASK-0020 (beta version stamp). The supply chain landed with TASK-0158 ✅; only the SBOM remains here, where it attaches to a tagged release
- [x] TASK-0170: `pip install fasterrag` was unusable out of the box — every command needs a `config.yaml`, and the missing-config error told the user to "copy the canonical config.yaml from the repository root", which an installed package does not have. Added `fasterrag config init`: it writes the canonical config plus `.env.example`, force-includes both into the wheel so the template cannot drift from the documented one, refuses to overwrite without `--force`, and never writes `.env` itself. Verified against a built wheel in a clean venv from an empty directory: `doctor` → names the command → `config init` → `config validate` passes — ✅ 2026-08-02

### Audit follow-ups (first formal audit, 2026-08-02)

- [x] TASK-0156: Fail fast on accepted-but-unenforced config — `security.auth`, `security.multi_tenancy`, and both `cost.*_token_budget` keys validate today and are consumed by nothing; enabling any of them must raise `ConfigError` naming the missing slice (mirror the implemented `cache.backend: redis` pattern) until enforcement lands with TASK-0046 — `_reject_unenforced_settings` runs in `load_settings` before the env-var check, lists every enabled key at once so an operator learns all of them in one restart, and leaves `0` (the documented "unlimited") accepted; the canonical config is asserted to still start — ✅ 2026-08-02
- [x] TASK-0157: Enforce the coverage gate in CI — `--cov-fail-under=85` scoped to `core/`, `adapters/`, `workers/`; the 87% measured today is met by discipline, not by a gate — added as its own CI step after the unrestricted run. Scoped deliberately: a repository-wide threshold is diluted by CLI plumbing and API wiring, so it can stay green while the retrieval and adapter code it exists to protect regresses. Re-measured before setting it at 87% branch coverage over 3525 statements — ✅ 2026-08-02
- [x] TASK-0158: Supply chain per security.md §6 — `uv.lock` committed (135 packages, 1577 sha256 hashes), `uv lock --check` gates CI and was verified to exit 1 on drift and 0 when clean, gitleaks scans full history, and the dependency audit is now **blocking** rather than advisory. The audit runs against the lockfile export rather than an installed environment — those differ, and only the lockfile describes what every machine installs — and measured clean across all 135 packages. The SBOM stays with TASK-0087, where it attaches to a tagged release — ✅ 2026-08-03
- [ ] TASK-0159: Implement the `cache_only` degradation rung — consult the semantic cache when retrieval raises, serving `degraded: true, mode: cache_only` instead of a bare `RETRIEVAL_FAILED`; pairs with the breaker (TASK-0148) and the scope decision (TASK-0165)
- [x] TASK-0160: Add a Windows CI leg running the fast suite — the project is developed on Windows and TASK-0141's path-case defect is exactly the class ubuntu-only CI cannot catch — added as a `windows-latest` job running the fast suite only; lint, format, and typecheck are not repeated because they are platform-independent and already gate on the quality job. Two shipped defects were Windows-only: the path-case document id and the UTF-8 BOM the config loader rejected — ✅ 2026-08-02
- [x] TASK-0161: Add a nightly scheduled CI job running the eval and benchmark suites with ledger-format artifacts, so the provable-claims policy is infrastructure rather than habit — `.github/workflows/nightly.yml` runs the eval and benchmark suites at 03:00 and uploads ledger-format artifacts with 30-day retention. The workflow states in its own header that its numbers are **not citable**: a shared GitHub runner has neighbours, and performance.md requires an isolated documented machine. It is a regression signal; TASK-0084 still needs real hardware — ✅ 2026-08-03
- [x] TASK-0162: Add a CI check diffing the FastAPI-generated OpenAPI schema against api-reference.md's endpoint table, catching endpoint drift automatically (the `traces list` drift was caught by hand) — `scripts/check_openapi_drift.py`, wired into the quality job. It found seven real discrepancies on its first run: `GET /v1/traces` and `POST /v1/retrieve` served but undocumented, and five documented endpoints nothing serves. All seven are now resolved — the two are documented, and the five carry an explicit **Not yet implemented** marker that the gate honours, so a reader sees the gap rather than the script merely tolerating it. It also catches the reverse drift: an endpoint that got built while the marker was left behind — ✅ 2026-08-03
- [x] TASK-0163: Implement the `FasterRag` facade per the python-api.md status table — `from_config`, `from_settings`, async context manager, `ingest`, `query`, `query_stream`, `retrieve`, `estimate`, `index_lock`. A composition layer only: it builds the same services `api/dependencies.py` builds, so no behavior can exist here that the REST API lacks, and a test asserts no pipeline logic crept in. Exported lazily through `__getattr__` — `import fasterrag` stays at 6 ms while resolving the facade costs 4.4 s. Verified end to end against live Qdrant and OpenAI: estimate 3 docs/189 tokens, ingest 3 indexed, retrieve 3 hits, query answered with a citation, stream 59 events `meta`→`done`, lockfile present — ✅ 2026-08-03
- [x] TASK-0171: `FasterRag.collections`, `.doctor`, `.replay`, and `.export_archive`/`.import_archive` are still unimplemented on the facade; the CLI and REST surfaces cover them today. The python-api.md table marks each one — `doctor`, `collections`, `create_collection`, `drop_collection`, and `replay` on both facades. `create_collection` takes the dimension from the embedding router rather than an argument, because a collection whose width disagrees with the model writing into it fails on the first upsert. `replay` runs with no trace store and no cache, so investigating an incident cannot alter the evidence. `doctor` works unstarted — it exists to diagnose installations that cannot start. Archives remain unwrapped because TASK-0079 has not built them — ✅ 2026-08-03
- [x] TASK-0172: `fasterrag.sync` blocking facade — the documented wrapper over the async one, for callers not running an event loop. Owns its event loop (created on `__enter__`, closed in a `finally` so a shutdown failure cannot leak it), refuses to start inside a running loop rather than deadlocking, and keeps `query_stream` incremental by pulling one event at a time — measured first token at 0.91 s over a 51-event response. Verified live: retrieve 3 hits, query answered in 4.30 s with a citation, stream `meta`→`done` — ✅ 2026-08-03
- [x] TASK-0174: The shipped `config.yaml` defaults to `retrieval.rerank: true` with `BAAI/bge-reranker-v2-m3`, a **2.2 GB** cross-encoder. A fresh `pip install` user's first query therefore downloads 2.2 GB and then loads it — measured as **not completing within 400 s** on a developer laptop, presenting as a hang with no output. Two things to decide: whether the out-of-box default should be a small reranker (or `rerank: false`) with the large model as an opt-in, and whether model loading should emit progress so a first run looks slow rather than broken. Found while verifying the sync facade, where it masqueraded as a facade bug — the *silent* half is fixed: the first-load message is now a **warning**, not an info line, because nothing configures logging in a plain script and Python's last-resort handler only emits warnings. It names the cost and the two ways out (`retrieval.rerank: false`, or a smaller `retrieval.reranker_model`), and the load duration is logged on completion. The **default** half is deliberately left open as TASK-0175: whether to ship a smaller reranker out of the box is a retrieval-quality decision for the maintainer, not a bug fix — ✅ 2026-08-03
- [x] TASK-0176: `normalise_source` (TASK-0141) changed the document id of every path-based source, so collections indexed before it no longer match ids computed after it — observed as `handbook_baseline` scoring a flat 0.0 until re-ingested. Added `IDENTITY_VERSION`, recorded in `index.lock` and compared by `detect_drift`, so the mismatch is *reported* by `index lock verify` rather than presenting as a broken retriever. A lockfile written before the field existed defaults to scheme 1, never to the current value: defaulting to "current" would declare a stale index up to date and hide the very mismatch the field was added to surface. 7 tests — ✅ 2026-08-03
- [x] TASK-0177: Filed on a broken diagnostic, not a defect. The parser is correct: `parse_bytes` on the affected file yields codepoint `U+2014`, an in-process and a spawned-subprocess `parse_and_chunk` both preserve it, an adapter round-trip returns the string byte-identical, and re-reading the stored payload with an explicit UTF-8 decode shows `em_dash=True mojibake=False` on all 12 documents. The mojibake was in my check: piping curl into `json.load(sys.stdin)` decodes with the Windows console codepage rather than UTF-8, so correct UTF-8 bytes were mangled on the way in. Entry kept rather than deleted so the ledger records the correction — ✅ 2026-08-03
- [ ] TASK-0175: (maintainer decision) The shipped default is `retrieval.rerank: true` with `BAAI/bge-reranker-v2-m3` (2.2 GB, measured at over 400 s to load on a laptop). Reranking is expected to be the strongest quality lever in the stack — expected, not measured (TASK-0084) — so turning it off by default trades an unquantified quality gain for a faster first run. The alternatives are a smaller default cross-encoder, or keeping it and treating the first-run cost as documented. TASK-0174 made the wait visible; this decides whether it should exist
- [x] TASK-0173: Entry-point plugin groups (`fasterrag.vectordb` / `.embeddings` / `.llm`); the factories resolve built-ins only. Split out of TASK-0163, which shipped the facade half — the premise was stale: all three factories already resolved entry points. Verified rather than assumed, with 15 tests registering a fake plugin against each group — resolution, origin reporting, rejection of a non-adapter, and the rule that a built-in name always wins so an installed package cannot silently take over a configured provider — ✅ 2026-08-03
- [x] TASK-0166: Add a CI self-truth check that fails when CLAUDE.md or README claims a documentation-only state while `src/` exists — the audit found exactly that inversion — `scripts/check_doc_truth.py`, wired into CI. Deliberately narrow: it asserts only the cheap-to-verify, expensive-to-get-wrong facts and ignores fenced code blocks, because a doc gate that fires on judgement calls gets disabled. Parameterised by root so the gate itself is tested — eight tests, including a reconstruction of the audit's actual inversion. Left out of pre-commit at the maintainer's direction; CI-only — ✅ 2026-08-02
- [x] TASK-0167: Run `fasterrag doctor --json` as a CI smoke job against the CI environment itself (dogfoods D10 on every push) — added to the quality job, and it exercises the packaged-template path with it: `config init` in an empty directory, then `doctor --json`. Placeholder keys are set so doctor gets past config validation and actually runs its environment checks; without them it would exit 4 on the first one and pass trivially. Exit 4 is accepted afterwards (CI has no Docker daemon, and reporting that is doctor working); a crash still fails the job — ✅ 2026-08-02
- [x] TASK-0168: Turn the generation price table into dated data with an unpriced-traffic counter metric so the cost panel can say "some traffic unpriced" instead of silently understating (extends TASK-0152) — added `fasterrag_unpriced_tokens_total{provider,model}`. An unpriced model previously contributed nothing to the cost total, which made the dashboard understate spend while looking healthy; the panel could not say "some traffic is unpriced" because nothing counted it. The cost panel's description now states that unpriced traffic is excluded and that a non-zero counter means the figure is low — ✅ 2026-08-03

### Claims audit follow-ups (second provable-claims audit, 2026-08-09)

- [x] TASK-0238: Execute the periodic provable-claims audit CLAUDE.md mandates — ✅ 2026-08-09 (**scope**: all 33 files under `docs/` including `adr/`, plus `README.md`, `CLAUDE.md`, and every docstring, module header, and `# CRITICAL:` block under `src/` — roughly 4,800 lines of prose and ~230 source modules. **Method**: five sweeps, each run rather than eyeballed. (1) A superlative regex over every doc (`fastest`, `most efficient/optimized/reliable`, `best-in-class`, `unmatched`, `industry-leading`, `state-of-the-art`, `highest`, `biggest`, `single largest`) — 16 hits, of which 8 were the provable-claims policy quoting itself and 3 were explicit goals ("aims to be", "its goal is to be"), both compliant; **8 were unbacked claims and all 8 were corrected**. (2) A verification-language sweep (`verified`, `confirmed`, `measured`, `benchmarked`, `proven`) over docs and `src/` — ~60 hits, most legitimately tracing to a ticked ledger entry; **9 were rewritten** to say that the check was a one-off manual run rather than a committed regression test, which is what they actually were. (3) A provenance sweep for bare numbers. (4) A **scripted** dangling-id check comparing every `TASK-`/`AUDIT-`/`BENCH-` reference in the tree against the ids this ledger defines. (5) A declaration-versus-implementation check of every capability the docs describe as shipped or unshipped, against `src/`.
  **Headline finding — one fabricated number had propagated to nine files.** "Reranking adds ~100–300 ms per query" and its companion superlative "the single biggest quality lever" appeared as plain fact in `architecture.md`, `scope.md`, `glossary.md` (which pins *canonical* definitions), `config-reference.md`, `flow.md`, `troubleshooting.md` (twice), `integrations.md`, `ADR-0004`, and `blockers.md`, and `performance.md` cited it circularly as "the documented 100–300 ms band". **It traces to nothing**: no ledger entry isolates the rerank stage, no `references.md` row carries a rerank latency, and the ledger's only two entries are both marked non-citable. It could not be verified cheaply either — the default cross-encoder is 2.2 GB and TASK-0174 ✅ measured it at over 400 s to load. **Withdrawn everywhere**, with each site now saying the stage is unmeasured and pointing at TASK-0084.
  **Second finding — stale negative annotations, in both directions.** The 2026-08-02 audit annotated docs "specified-not-built"; the slices that then built those things (TASK-0045/0046/0132/0148/0150/0151/0173/0179/0189/0214/0232) did not remove the markers. **Nineteen capability statements were wrong**: the observability dashboard, API auth, multi-tenancy, the circuit breaker, OTLP trace *and* metric export, contextual enrichment, entry-point plugins, the pgvector adapter, Langfuse/Grafana startup triggering and doctor gating, and the D11 archive were all documented as unbuilt while shipping. The most dangerous was `cookbook.md` R5, which told operators that flipping `security.auth: true` "changes nothing, and the API serves unauthenticated" — advice that would have led someone to leave a *working* control switched off. Corrected, and `failure-modes.md`'s as-built note, which asserted the circuit breaker was unimplemented, was inverted back.
  **Third finding — capabilities documented as shipped that do not exist.** `cache_only` (the middle degradation rung) appears nowhere in `src/` yet was described as built in six places, including the chaos-suite table D12 cites as its proof. The runtime cost governor does not exist: `BUDGET_EXCEEDED` is defined and raised by nothing, `estimated_cost_usd` is in no response, and **three cookbook recipes shipped configs that raise `ConfigError` at load** — R9, the recipe aimed at first-time evaluators, also named `chroma`, an adapter that is refused by name. The load, soak, and mutation test layers are documented as required parts of the pyramid and **do not exist** (no k6/Locust script, no soak suite, `mutmut` not a dependency; the `load`/`soak`/`mutation` markers are declared and used by zero tests) — four FMEA rows name one of them as their proving test. `performance.md` claimed nightly runs "append to the ledger" and file regressions "automatically"; the nightly workflow uploads artifacts and does neither.
  **Dangling ids**: 6 found, 2 of them deliberate test fixtures (`BENCH-0007`, `TASK-9999`) and left alone; **4 were real** — `TASK-0221`/`TASK-0222` cited in `pgvector.py` and `TASK-0225` in `qdrant.py` pointed at ids this ledger has never held. The qdrant one is a *true* claim wearing a wrong id (the measurement is TASK-0226 ✅) and was repointed; the pgvector two named real pending work with no task behind it, now filed as TASK-0239/TASK-0240.
  **Also corrected**: `README.md` principle 5 still told contributors to use feature branches, contradicting the trunk-based rule CLAUDE.md has carried since 2026-07-30; `reliability.md` carried TASK-0153 as an open timeout violation when that task was closed as a mis-read with no defect; `security.md` §6 still claimed non-root for *every* provisioned image, the exact wording AUDIT-0007 ✅ narrowed in `deployment.md` and never propagated; `migration-guide.md` claimed "we ship six vector DBs" (two) and that every number in the docs is TBD-marked (at the time, at least seven were not).
  **Also found while rebasing**: TASK-0178 was still open and still asserting "no real Langfuse has accepted a batch" after TASK-0236 ✅ had done exactly that work earlier the same day — ticked here, and `blockers.md`'s B7 heading repointed at TASK-0079, the id that genuinely still blocks that group.
  **Totals: 91 edits across 30 files** — 3 source files (docstrings and one `# CRITICAL:` block only; **no behaviour was touched**, and the unit suite reports the same 2029 passed / 6 skipped before and after) and 27 documents. **Nine claims could not be resolved and are filed as open tasks below** (TASK-0239 through TASK-0247). **Two judgement calls recorded rather than acted on**: "the fastest way to see what a deployment customised" in `cli-reference.md`, `cli/commands/diagnostics.py`, and `api/query.py` is idiomatic English about which command is quickest to type, not a performance claim about the software, and was left alone; and `disaster-recovery.md`'s 4,311 ms restore figure was left because the document already refuses to publish it as an RTO and states its scope, which is the behaviour the policy wants. `CHANGELOG.md` was given a standing provenance banner rather than having its dated historical observations rewritten)
- [x] TASK-0239: Pool connections in the pgvector adapter — ✅ 2026-08-11 (it held one connection and serialized every operation on it, which its own module docstring admitted. In an async, multi-worker system that makes the adapter a global lock on the whole vector-DB path: one blocked statement stalls every other caller, including a `health()` that runs `SELECT 1` and needs no lock at all. Now a `psycopg_pool.AsyncConnectionPool` sized by `vector_db.pgvector.pool_max_size` (default 8, per process). **Proved by construction rather than by timing**, which matters because a stopwatch on a laptop running Docker measures the laptop: an external session takes an `ACCESS EXCLUSIVE` lock on the collection's table, so an adapter statement touching it blocks for exactly as long as the test chooses — no sleeps and no races. Mutation-verified against a live PostgreSQL 17 + pgvector: forcing the pool back to size 1 fails the concurrency case. No speedup is quoted anywhere, because this hardware fails benchmark-ledger rule 5)

- [x] TASK-0240: Give the pgvector adapter a statement timeout that distinguishes queries from index builds — ✅ 2026-08-11 (only the connect was time-bounded, so a slow query or a runaway index build could hang indefinitely. Ordinary statements now carry `reliability.timeouts.vector_db_ms` as a session `statement_timeout`; schema changes, snapshot copies, and restores carry `vector_db.pgvector.maintenance_timeout_ms` (default 600s) applied with `SET LOCAL`, so the longer budget cannot leak back to the pool on the connection that used it. **The split is the whole point and is what the test pins**: a bound tight enough for a query aborts the HNSW build on any collection large enough to need one, so giving maintenance the query budget fails the case where a blocked snapshot must outlive it — mutation-verified exactly that way. The longer budget is still a bound, not an absence of one, pinned by a second case where a blocked snapshot is cancelled at it. Both settings live under `vector_db.pgvector` rather than `reliability` because a connection pool and the `statement_timeout` GUC are PostgreSQL concepts no other adapter can honour, and a shared key only pgvector reads would be silently ignored by every Qdrant deployment)

- [ ] TASK-0241: Write the chaos and reindex scenarios the docs name but the suite does not contain — **config-mutation** (D1's proof metric asserts zero false negatives across scenarios that do not exist), **network-partition injection** (FMEA rows 16 and 35 name it as their proving test), and the **continuous-query-load-during-reindex** integration test D2's acceptance test specifies. Until they exist, those three claims are annotated as targets rather than results
- [x] TASK-0242: Implement the D9 runtime cost governor — ✅ 2026-08-11 (built rather than narrowed: `services/budget.py` enforces both budgets before the provider is called, and `BUDGET_EXCEEDED` is now raised by something. **Budgets are checked against the worst case, not the likely one** — the prompt's real size plus `llm.max_tokens`, because a completion's length is unknown until the provider has produced it and by then the money is spent; a cap that admits a request it cannot pay for is not a cap. The tenant is then charged what the query *actually* cost, at the single point both the streaming and non-streaming paths pass through: admitting on the ceiling and charging the actual is the pair that neither overspends nor over-refuses. **The placement of the check is the subtle part.** It sits *outside* the try block that implements the degradation ladder, because that block absorbs retryable failures into an extractive answer — a retryable budget error would have turned a spend control into a quality regression nobody could see. `BUDGET_EXCEEDED` is already non-retryable in the problem table, so it would re-raise regardless; keeping the call outside means that stays true even if the ladder's conditions are ever loosened. **A caveat is documented rather than hidden**: the per-tenant counter is in-process, exactly like the per-key rate limiter (TASK-0216), so N replicas grant N times the budget — and for a spend cap that error runs in the permissive direction. It ships anyway because a single-replica deployment gets precisely what it configured and because refusing to start taught operators nothing, but security.md, cookbook.md and config-reference.md all now say so outright. The rolling window is fixed at 24h and documented, since "rolling" without a period means nothing. The empty `_reject_unenforced_settings` guard was retired rather than kept as a no-op with an unused parameter, its two refusal tests converted to acceptance tests following this file's own precedent for auth and tenancy, and the rule it embodied moved into the loader's module docstring. 11 governor tests plus 5 end-to-end; seven mutations, all caught — **one only after being rewritten**: the ceiling check survived its first test because a budget below the prompt size refuses either way and cannot tell prompt-only from prompt-plus-ceiling apart, so the budget now sits deliberately between the two)

- [ ] TASK-0243: Build the three testing-pyramid layers that are documented as required and do not exist — **load** (k6/Locust, hardware-annotated), **24 h soak** (no memory/fd growth), and **mutation testing** (mutmut on chunk-boundary and retrieval-scoring logic). The `load`, `soak`, and `mutation` markers are declared in `pyproject.toml` and carried by zero tests, and four FMEA rows cite one of these suites as the test that proves them. Mutation checking is currently done by hand per slice, which is the practice this would automate
- [ ] TASK-0244: Wire the D7 retrieval regression gate into CI. `services/regression.py` and `fasterrag benchmark --suite eval` implement the tolerance comparison, and `testing-strategy.md` lists it among the blocking gates, but no CI step runs it against the committed baseline — so the gate that exists to stop a worse RAG shipping is not currently in the path of a push
- [x] TASK-0245: Consult the `llm` circuit breaker — ✅ 2026-08-10 (the breaker existed and nothing read it, so reliability.md's "one breaker per provider" overstated the protection and FMEA row 20's "retries → breaker opens → extractive" was only ever retries. Verified worse than filed: `create_breakers` was reachable **from tests alone** — no production code called it, so the `llm` series never reached the `fasterrag_circuit_state` gauge at all, and an absent series reads as healthy on every dashboard. `GenerationService` now owns a breaker for its whole life (rebuilding one per query would count to one forever and never open), checks it *inside* the existing try so an open circuit takes the same route a live outage takes — `CircuitOpenError` is retryable by construction, making load-shedding a rung on the ladder rather than a 503 — and records failure and success on both the `complete` and `stream` paths. The dead `create_breakers` factory was removed rather than wired: its stated purpose, publishing `closed` from startup, is already met by `CircuitBreaker.__post_init__`, confirmed by constructing a service and watching the `llm` series appear on the gauge. Four tests, all three hooks mutation-verified — the first version of the success test was vacuous (one failure below threshold leaves the breaker closed whether or not the count is cleared) and now runs fail → succeed → fail against a threshold of two, so only clearing keeps it shut)

- [ ] TASK-0246: Make nightly regression detection real, or stop describing it. `performance.md` §3 claimed nightly runs append to the ledger and file a bug in this file automatically on a >10% p95 regression; the workflow uploads artifacts with 30-day retention and does neither. Both halves are defensible to build (a comparison step and an issue-filing step) or to drop — what is not defensible is the doc describing automation that a reader would rely on
- [ ] TASK-0247: Cite the R3 chunk-size preprint properly or withdraw the numbers it backs. The `references.md` row names no title, no authors, and no link, so neither the "~2,500-token context cliff" nor the "~512–1024-token working range" can be checked by a reader or a reviewer — and both are quoted as guidance in `architecture.md`, `config-reference.md`, `glossary.md`, and `scope.md`. Rule 2 of that file requires numbers quoted exactly as the source states them, which is unverifiable against a source nobody can open

### Release-readiness review follow-ups (2026-08-10)

- [x] TASK-0251: Serve Langfuse and Grafana provisioning over REST — ✅ 2026-08-11 (reproduced on `main` first: `api/admin.py` held `_PROVISIONABLE = frozenset({"qdrant"})` while `docs/api-reference.md` line 226 documented `qdrant`, `langfuse`, *or* `grafana` as supported, so `POST /v1/admin/provision/langfuse` answered NOT_FOUND against a doc that promised it. Neither an endpoint test nor the OpenAPI drift gate could see it — the route existed and was served, and the disagreement lived in a *value inside it*, which is why the release-readiness review found it by hand and the automated gates did not. Fixed structurally rather than by widening the set: `services/provision_registry.py` is now the one table of what can be provisioned and the three verbs each tool answers, and the CLI's `choices=`, the REST surface, and the error message listing what is supported all read it — there is nothing left to keep in sync. Salvaged from a worker stopped mid-verification, and it was **incomplete in a way that mattered**: it had written the `DELETE /v1/admin/provision/{tool}` row into api-reference.md and a test exercising the `down` verb, but never added the route, so the doc described an endpoint that answered 405 — documented-before-built, which CLAUDE.md forbids. The CLI has `--down` for all three tools, so parity genuinely required it and it was implemented rather than the doc narrowed. It stops containers only: data volumes and generated secrets survive, because regenerating Langfuse's `SALT`/`ENCRYPTION_KEY` would invalidate every credential issued against them. Not tenant-scoped — a container is one process-wide resource, so scoping would imply a per-tenant stack that does not exist — and guarded by the `admin` scope the middleware applies to the whole `/v1/admin` prefix. Mutation-verified: reverting the registry to qdrant-only fails 6 tests, removing the DELETE route fails 7. OpenAPI drift gate green)

- [ ] TASK-0255: Gate `docs/internals.md`'s spec→module map against the real tree, the way `tests/unit/docs/test_reference_parity.py` gates the CLI and config references — a module named in the map that no longer exists, or a new top-level module absent from the map, should fail the docs suite. The map was hand-verified against a fresh `find` on 2026-08-10; without a gate it decays the way every hand-verified list here eventually has (the stale `# TODO:` of TASK-0251 being the latest example)
- [ ] TASK-0256: Delete the pre-release banner in `docs/how-to-use.md` and the "not yet published" rows in `docs/python-api.md`/README as part of release step 3 in `docs/release.md` — filed now so the release checklist cannot forget the docs that promise to update themselves
- [ ] TASK-0252: Reconcile the coverage gate. The gated packages (`core/`, `adapters/`, `workers/`) measured **83% branch coverage on the 2026-08-10 dev machine** against the **85** threshold CI enforces (`--cov-fail-under=85`), and ~5,900 gated-package statements have been added since the gate was set at the 2026-08-02 measurement of 87.11%. Establish which of three states is true — the Linux CI run measures ≥85 and the platforms diverge by ≥2 points (then record the platform dependence next to the gate), CI on main is currently red (then main is not green and the trunk rule is breached), or the gate step's scope drifted — and then restore headroom above the threshold rather than lowering it, per the gate's own rationale in testing-strategy.md §2

## Future

- [ ] TASK-0051: Multi-modal ingestion (images, audio) exploration
- [ ] TASK-0052: Distributed multi-node worker orchestration (Kubernetes operator)
- [ ] TASK-0053: Additional adapters (Elasticsearch, Vespa, LanceDB) via community entry-point contributions
- [ ] TASK-0054: GraphRAG / knowledge-graph retrieval exploration
- [ ] TASK-0055: Managed cloud offering feasibility study (post-beta)
