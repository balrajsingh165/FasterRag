# Secrets — never commit (see docs/security.md)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
coverage.xml
htmlcov/
.mutmut-cache

# Runtime artifacts (build phase)
*.log
*.tmp
autopilot-suggestion.yaml

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Runtime state written by a running deployment, never source
# (ingestion journal, dead letters, dedup hashes, and the disk embedding cache)
.fasterrag/
